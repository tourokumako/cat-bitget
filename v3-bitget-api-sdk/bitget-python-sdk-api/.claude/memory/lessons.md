# lessons.md — 過去の失敗・教訓（V8移行時含む）

### L-113: TRAIL_EXIT の `trail_net ≈ ratio × final_mfe - fee` 仮想シミュは大外しする（2026-04-24）

**何が起きたか:**
P23 の TIME_EXIT giveback（35件 -$2,102・avg giveback $78）を TRAIL_EXIT で捕捉する提案。非TP_FILLED置換の仮想シミュで「activation=$15・ratio=0.8」→ **+$14.62/dt-day** を予測。P23 の優先順位リストに 23 を追加（デフォルト `P23_MFE_GATE_PCT=0.05` / `P23_TRAIL_RATIO=0.8` 継承）して 365d Replay 実行。結果 P23 $21.32 → **$1.18/dt-day（-$20.14悪化）**。TP_FILLED 47件→1件（-46件・L-111そのもの）、STOCH_REVERSE_EXIT 41件消滅、avgHold 100分→9.5分。即ロールバック。

**仮想シミュの欠陥:**
`trail_net = ratio × mfe_final - fee` という近似は「TRAIL は peak MFE まで成長してから retrace で確定する」と仮定していた。実際の TRAIL 実装は `_fav_pct >= P23_MFE_GATE_PCT` で即活性化するので、デフォルト GATE_PCT=0.05（≈MFE $1）では **価格がわずかに順行した瞬間に活性化、ratio=0.8 で 20%retrace → ほぼ全 trade が MFE=$5〜$10 で TRAIL_EXIT される**。TP_PCT=0.012（≈MFE $96）到達より遥か手前で利確されてしまい、TP_FILLED の長ホールド構造が成立しない。

**Why:**
TRAIL_EXIT は「MFE ピークがある程度育ってから retrace を検出する」のではなく、「活性化後は常に `peak × ratio` で stop を更新する」ratchet型ロジック。GATE が小さいほど早期活性化し、ratio が高いほど小さな retrace で発火する。P21（8分avg hold・小MFE型）にはフィット、P23（100分avg hold・大MFE型）には壊滅的にアンマッチ。シグナルの性格（短期決着 vs 長時間育成）と TRAIL パラメータの相性を見誤った。

**How to apply:**
- TRAIL_EXIT を新規 Priority に有効化する時は、**既存の `P21_MFE_GATE_PCT=0.05` をそのまま継承してはならない**。Priority 固有の TP_PCT・avgHold・MFE分布に合わせて GATE_PCT を先に設計する（例: P23 なら TP=1.2% の半分 0.6% 以上を起点に検討）
- 仮想シミュの `trail_net ≈ ratio × final_mfe - fee` 近似は **ratchet 型 Exit の動的挙動を全く捉えない**。TP_FILLED の avgHold と TRAIL 活性化タイミングの前後関係（TRAIL 活性化 → 小retrace発火 → TP到達前に退場）を必ず手で追ってから採用判定する
- PROFIT_LOCK/TRAIL/MFE_DRAWDOWN_CUT など途中 unreal に依存する Exit は **L-111 / L-112 の原則通り、CSV 仮想シミュでは採否判定できない**。必ず最小変更で Replay → 結果確認 → パラメータ詰め、の順序。大改修を一度に乗せない

---

### L-112: final_mfe_usd から「途中時点のMFE」を逆算してはならない（2026-04-24）

**何が起きたか:**
P23 の add>=1 への MFE_STALE_CUT 拡張を提案（`P23_MFE_STALE_ADD_MIN=1`）。プリシムで「add>=2 TP_FILLED 32件の最小 final_mfe = $18.4 > gate $4 → TP奪取ゼロ」と予測し +$2.45/dt-day 改善を見込んだ。365d実Replay結果: P23 $18.74→**$11.37/dt-day（-$7.37悪化）**。TP_FILLED 45件→40件（5件奪取）、STOCH_REVERSE_EXIT 48件→36件（12件奪取）、MFE_STALE_CUT 61件→110件（予想+7件だったが実際+49件）。即ロールバック。

**プリシムの欠陥:**
「`final_mfe = $18` の trade は MFE@30min も $18近辺」と無意識に仮定した。これは誤り。**MFE は単調増加型ratchet指標**なので、`final_mfe = $18` の trade でも `MFE@30min = $2` は十分あり得る。30min時点でゲートを切ると、その後 MFE が成長して TP に到達する trade まで奪取してしまう。

**Why:** MFE = max(unreal_usd over time)。初期フェーズで低く、価格が順行するほど拡大する。add>=2 trades は特に「価格逆行 → add累積 → 反転 → MFE拡大 → TP」というシナリオを設計前提にしている。30min時点の低MFEは**多くの trade にとって正常状態**。

**How to apply:**
- CSV の `mfe_usd` 列は exit 時点の最終値のみ。「途中時点の MFE」は推定不可能
- MFE_STALE / PROFIT_LOCK / TRAIL / MFE_DRAWDOWN_CUT 等「途中 MFE に依存する exit」は final_mfe を使った仮想シミュでは TP奪取の評価ができない。実測ランでのみ検証すること
- add>=2 に MFE_STALE を拡張するのは**マルチadd戦略の設計思想と矛盾**。add は逆行時の蓄積のためにある。早期カットは add=1（単発失敗検知）の文脈にのみ妥当

---

### L-111: PROFIT_LOCK仮想シミュはTP_FILLED奪取を見落として大外しする（2026-04-24）

**何が起きたか:**
P23のTIME_EXIT TYPE II（MFE≥$15で戻った20件・$-1,723）をPROFIT_LOCKでCapture する提案。仮想シミュで ARM=20/LOCK=5 → +$29.13/dt-day と予測したが、365d実Replay結果: +$7.00/dt-day（ベースライン$18.74から-$11.74悪化）。全12パターン REJECT。

**仮想シミュの欠陥:**
「MFE≥ARMに到達したTIME_EXITがPROFIT_LOCKに置換される」と仮定した。しかしTP_FILLED 45件もほぼ全件MFE≥15を通過しており、TP到達前にunreal<LOCK の瞬間があればPROFIT_LOCKが先に発動。
- TP率: 22.5% → 17.5%（-5pt、TP avg$88がPROFIT_LOCK avg$3に置換）
- TIME_EXIT削減効果（43→28件=-15件）はTP奪取の損失を全く補えない

**Why:** PROFIT_LOCKは「MFE≥ARM AND unreal<LOCK」のみで判定するため、TP_FILLEDに向かう途中の一時的な押し戻しを捕まえてしまう。STOCH_REVERSE_EXITはstoch crossという独立シグナルを要求するのでTP奪取しない。設計思想が違う。

**How to apply:**
- PROFIT_LOCK/TRAIL系のExit追加を仮想シミュする時は、**TP_FILLED／STOCH_REVERSE_EXITトレードのMFE分布**も必ず確認し「切り替わり対象のはずのexit_reason」以外への波及影響を推定する
- 「unrealの途中経過」が必要な Exit（PROFIT_LOCK, TRAIL, MFE_DRAWDOWN_CUT 等）はCSVだけでは仮想シミュ不可能に近い。ARM/LOCK値はSTOCH/TP等の既存Exit発動条件と重ならないか必ず確認してから提案する
- 仮想シミュ結果を根拠に「理論最大+$10/dt-day」と断言しない。見落としの可能性を明示する

---

### L-110: 180dの外れ値依存最良値はOOSで逆転する（ATR_MAX=500→300逆転）（2026-04-23）

**何が起きたか:**
P2 ATR_MAX探索で180d Best=500（+$3.46/dt-day）だったが、365d OOSでBest=300（+$1.08）に逆転した。ATR500は180d内のSL_FILLED1件（ATR=514, -$218）を除外する効果だけで上位になっていた。

**Why:** 180dに1件だけ含まれる大損失(-$218)の除外効果がATR_MAX=500の「$/dt-day」を水増しした。365dではこの比率が希薄化し、ATR250〜300の適切なフィルター幅が正しく評価された。

**How to apply:**
- 180d最良値を即採用しない。必ず365d OOSで確認してからbestを決める
- グリッドサーチで1件の外れ値が支配的なパターン（件数が少ないのに$/dt-dayが飛び抜けている）は疑ってOOS確認を優先する

---

### L-109: STOCH_REVERSE_EXIT の MIN_HOLD は単調増加せず谷が存在する（2026-04-23）

**何が起きたか:**
MIN_HOLD=60→90→120→150と探索したところ、HOLD=90で一度-$2.62/dt-dayの谷を形成し、その後HOLD=120/150で再び上昇した。最終的にHOLD=150が+$16.01/dt-dayでグローバル最適。

**Why:** Stochクロスは60-90分帯に「TP到達寸前を誤カットする悪いクロス」が集中していた。MIN_HOLD=90はこの悪いクロス帯の真ん中に位置するため谷になった。MIN_HOLD=150以上になると悪いクロスをすべてスキップして良いクロス（TYPE II逆転）のみ捕捉できる。

**How to apply:**
- MIN_HOLD を増やせば単調改善とは限らない。必ずフルスキャン（今回は60/90/120/150/180/240/300/360/420/480）してから採用する
- HOLD=480（=TIME_EXIT_MIN）はベースラインと一致する「上限アンカー」として機能する

---

### L-108: Stochクロスは hold 中に複数回発生する（事前分析の見落とし）（2026-04-23）

**何が起きたか:**
事前分析で「MFE>15のTIME_EXIT 26件中、クロス時に含み益があるのは11件のみ。TP_FILLED 4件への影響も最初のクロスのみ確認した」と報告したが、クイックテスト（HOLD=30）でTP_FILLED が26→15に激減した。

**Why:** 240-480分保有するTIME_EXIT/TP_FILLED トレードでは、Stochは何度もゴールデンクロス・デッドクロスを繰り返す。事前分析が「最初のクロスのみ」を見たため、2回目以降のクロスによるTP破壊を見落とした。

**How to apply:**
- Stoch系 Exit を実装する前は「保有期間中の全クロス回数」を確認してから影響を推定する
- クイックテストは実装直後に必ず実施し、事前分析との乖離を確認してからグリッドサーチ設計する

---

### L-107: P23 per-add可変sizing（初回小・後半大）はTP_FILLED at add1を直撃して失敗（2026-04-23）

**何が起きたか:**
P23に[0.01, 0.03, 0.03, 0.03, 0.02]のper-add sizingを実装。TIME_EXIT 71→37件に削減できたが、$/dt-day $14.72→$10.01と大幅悪化した。

**Why:** 2つの構造的問題が同時に発生した。①TP_FILLEDはadd_count=1が最多（14/40件）なので初回0.01 BTCにすると利益が58%減。②STALE_GATE_USD=$4は絶対値のため、0.01 BTC初回だと「$4達成に0.44%必要（現在0.19%）」となり過剰発火（20→50件）。

**How to apply:**
- per-add sizingはSTALEと深く絡み合う。単体実装は不可。動的STALEとセットで設計する必要がある
- TP_FILLEDのadd_count分布を必ず確認してから初回サイズを設計する（初回を小さくすることが必ずしも有利ではない）

---

### L-106: 可能性を自分で見切らずデータで検証してから結論を出す（2026-04-23）

**何が起きたか:**
STALE設計変更について「TIME_EXIT主因には届かない（限定的）」と分析だけで判断し、実装・検証を提案しなかった。まこさんから「なんで勝手に見切るの？」と指摘を受けた。

**Why:** TYPE IIが78.9%という数字に引きずられ、「改善幅が小さいなら試す価値なし」と先に結論した。しかし現状のSTALE_CUTが実際には~+$360の価値を出していることは計算していなかった。

**How to apply:**
- 「効果が限定的」と判断しても、実装コストが低い改善は必ず試す
- 見切る前に「現状の仕組みが実際にどれだけ貢献しているか」を計算してから判断する

---

### L-105: add_countが多いPriorityの仮想シミュはReplay結果と逆転する（2026-04-23）

**何が起きたか:**
P23 ADX_MIN引き上げを仮想シミュ（per-trade CSVフィルタ）で評価した結果、ADX>=33 → +$1.28/dt-dayと出た。しかし実際のReplayではADX_MIN=33 → $6.44/dt-day（現状$12.81比 -$6.37）と大幅悪化した。

**Why:** 仮想シミュは「既存CSVのトレードをfilterするだけ」なので、ADX条件によって変わるaddの発火タイミングや追加エントリー数の変化を再現できない。P23はadd_countが平均3-4回と多いため、誤差が累積して正反対の予測になった。

**How to apply:**
- P23（およびadd_countが多い Priority）の仮想シミュ結果は「傾向参考」にとどめ、採用判断には必ず実Replayを実施する
- 仮想シミュとReplayが乖離した場合はReplay結果を正とする
- 単軸グリッドサーチ（4組程度）なら実Replayの方が速く正確

---

### L-104: サイズ最適化は損失源解決後が正しい順序（2026-04-23）

**何が起きたか:**
P23サイズ0.024→0.06（2.5倍）が「最優先改善軸」として浮上したが、P23 TIME_EXIT 68件/-$2,861が未解決のまま。サイズ増加すると損失も2.5倍（-$7,153相当）になる。

**Why:** 「実装コストが低い（JSON1行）」という容易さと「サイズ配分発見の興奮」が重なり、順序が逆転しそうになった。

**How to apply:**
- サイズ増加の採用条件を「TIME_EXIT件数が現状の半分以下」として先に設定する
- 損失源が未解決のままサイズを増やさない。整えてから拡大が原則
- replay_csv.pyはP{priority}_POSITION_SIZE_BTCのフォールバック機構済みなのでJSONのみで設定可能

---

### L-103: 監査でシステム固有の設計を確認せずに一般論を適用すると誤指摘になる（2026-04-23）

**何が起きたか:**
IS/OOS乖離（P21: $13.29→$6.24、53%劣化）を監査で問題指摘した。しかしP21はDOWNTREND限定エントリーであり、IS（90d中のDOWNTREND日）とOOS（365d中のDOWNTREND日）は同条件で比較されていた。「90dがたまたまP21に有利だった」という批判は成立せず、誤指摘だった。

**Why:** 「IS/OOS乖離はバックテスト全般の問題」という一般論をレジーム切り替え設計の確認より先に適用した。批判することが目的化し、前提検証が後回しになった。

**How to apply:**
- 監査で一般論（IS/OOS乖離・サンプルサイズ等）を適用する前に「この指摘の前提はXXXである。このシステムに当てはまるか？」を先に確認する
- auditor_template.md の Step 0（前提確認）を必ず実行してから各観点に入る
- 「批判できる」≠「批判すべき」。前提が崩れる批判は信頼を損なうだけ

---

### L-99: P21 MFE_STALE設定がデッドコードだった（2026-04-23）

**何が起きたか:**
cat_params_v9.json に P21_MFE_STALE_GATE_USD=4.0 / P21_MFE_STALE_HOLD_MIN=90.0 が設定済みだったが、replay_csv.py の _check_exits_replay に P21用 STALE_CUT ロジック（3e）が未実装だった。パラメータが完全にデッドコードになっており、TIME_EXIT=30件/-$550が垂れ流しになっていた。

**Why:** P2/P23/P3にはSTALE_CUTが実装されていたが、P21追加時に実装が漏れた。

**How to apply:**
- 新Priorityのパラメータを追加したら、対応するコードが _check_exits_replay に存在するか必ず確認する
- 「設定があるのに発動している形跡がない」は実装漏れのサイン

---

### L-100: STALE_HOLD を TIME_EXIT実効より短縮しても NET 改善しない（2026-04-23）

**何が起きたか:**
P21のTIME_EXIT（実効60min=120×DOWN_FACTOR 0.5）に対し、STALE_HOLD=30minで先に発動させようとした。
結果: STALE_HOLD=30 → $2.61/dt-day（ベースライン$5.65より大幅悪化）。30〜60minの間に一部トレードが回復しており早期カットが逆効果だった。

**Why:** TIME_EXITのavg_holdが60minでも、全件が60min固定ではなく一部は途中回復する。

**How to apply:**
- TIME_EXIT削減にはExit時刻調整より「Entry品質改善（フィルター追加）」が有効
- TIME_EXIT avg_hold == TIME_EXIT実効値のとき、STALE短縮は中性〜逆効果になりやすい

---

### L-101: ATR フィルターで「安値買いの薄利TRAIL」を削ると NET 改善する（2026-04-23）

**何が起きたか:**
P21_ATR14_MIN=60→150に変更。低ATR環境のTRAIL_EXIT（薄利多売）を除外したところ TIME_EXIT が 54→29件に減り、$/dt-day が 5.65→6.24（365d OOS）に改善した。

**Why:** P21はMACDクロスで動くが、低ATR時は値幅が小さくTRAIL利益も小さい。TIME_EXIT損失の絶対額は変わらないため、低ATR TRAILを削ることでNET/trade比率が改善する。

**How to apply:**
- ATRフィルターで「質の低い低ボラエントリー」を除外するアプローチは有効
- ただし ATR_MIN を上げすぎると有効TRAIL件数が減りすぎて逆効果（ATR>=200 → $5.44/dt-day）

---

### L-102: 仮想シミュレーションとグリッドサーチの順位が一致しないことがある（2026-04-23）

**何が起きたか:**
90d CSVの仮想シミュでATR=100が1位・ATR=150が3位。180dグリッドサーチではATR=150が1位・ATR=100が2位。順位が逆転した。

**Why:** 仮想シミュは90dデータ（Jan-Apr 2026）でATR=100が多く有効だったが、180d全体（Oct 2025〜Apr 2026）ではATR=150フィルターが前半期間に有効に働いた。期間の代表性が異なる。

**How to apply:**
- 仮想シミュは「あたり付け」として使う。グリッドを省略せず最終確認は実Replayで行う
- 仮想シミュの参照データが短期（好況期）だとフィルター効果を過大評価する場合がある

---

### L-97: _REGIME_PRIORITY_SETSにP1/P21が全レジームFalseで未割り当てだった（2026-04-22）

**何が起きたか:**
replay_csv.pyの_REGIME_PRIORITY_SETSでP21とP1が全レジーム(downtrend/range/uptrend/mixed/unknown)でFalseにハードコードされていた。regime_switch=ONにすると両Priority が完全に除外され、$9.9/dayに激減していた。

**Why:** P21/P1のレジーム割り当てが実装漏れのまま放置されていた。WORKFLOWの有効Priority表にも記載がなかった。

**How to apply:**
- 新しいPriorityを追加したら_REGIME_PRIORITY_SETSへの割り当てを必ず同時に行う
- セッション開始時にregime_switch=ONで全Priority有効のReplayを実行し、意図通りに発火しているか確認する

---

### L-98: P21_TP_BB_RATIO=0.0でTP=エントリー価格になり即発火バグを作った（2026-04-22）

**何が起きたか:**
P21「旧TP方式」比較テストで P21_TP_BB_RATIO=0.0 と P21_TP_MIN_PCT=0.0 を同時設定。
TP距離 = max(bb_half×0, price×0) = 0 → TP価格=エントリー価格 → 全トレードが即TP_FILLED・NET赤字。

**Why:** P21のTP計算がBBベース（TP_BB_RATIO × bb_half）+最小保証（TP_MIN_PCT）の組み合わせとわかっていなかった。

**How to apply:**
- P21/P1のTP無効化テストをする場合はMFE_GATE_PCT=999でTRAILをarmさせない方式を使う
- TP_BB_RATIOとTP_MIN_PCTは両方同時に0にしない

---

### L-91: 200SMAフィルター検証でデータ起点を誤り「効かない」と誤結論した（2026-04-21）

**何が起きたか:**
P3の200SMAレジームフィルター検証で、2025/4スタートの365d CSVから日足SMAを計算した。
その結果、SMA200が実際より高い値（$105-110K）になり「BTCはほぼ常にSMA以下」という誤った分析になった。
ユーザーがチャートを見せて指摘するまで気づかなかった。

**Why:**
- 200日SMAは「最新200日の平均」なので、計算起点から200日以上のウォームアップデータが必要
- 2025/4/1スタートのデータでは2025/10時点でも6ヶ月分しかなく「偽のSMA200」になった
- データの有効期間を確認せずに計算結果を信用した

**How to apply:**
- SMA200を使う場合、分析開始日の200日前のデータが必要かを必ず確認する
- 計算したSMA値をチャートの目視値と照合してから結論を出す
- ウォームアップ用データが不足する場合は先に取得する

---

### L-92: 200SMA否定後に新たな視点（チャート画像）で仮説が復活した（2026-04-21）

**何が起きたか:**
200SMAフィルター検証で「効かない」と誤結論した後、
ユーザーがチャート画像を提示して「200SMAが機能して見える」と指摘。
正しいデータで再検証したら SMA上方TP率12% vs SMA下方TP率31%（2.6倍差）という明確な効果を確認。

**How to apply:**
- ユーザーが視覚的な観察を提示したときは、データ分析の前提（データ起点・計算方法）を疑う
- 「効かない」結論を出す前に「計算自体が正しいか」を確認する

---

### L-93: 理論最大値を確認せずに改善打ち切りを宣言した（2026-04-22）

**何が起きたか:**
P23のATRフィルター・パラメータ調整が失敗 → 「P23は限界」と結論してP4分析に移ろうとした。
P23の理論最大値=$84.76/dt-day（目標$60超え）が目の前にあるのに確認していなかった。

**Why:**
「この手法が失敗した」を「このPriorityに改善余地がない」と誤って等号にした。
Step 0 D「理論最大値3行出力」が義務化される前の出来事。

**How to apply:**
提案・結論を書く前に必ず: `[Step 0 D] 目標/現状/理論最大→到達可能性` を1行出力する。
この1行なしの「限界」「打ち切り」発言は無効。

---

### L-94: GRIDにENABLE=[0,1]を入れて無意味な組み合わせを量産した（2026-04-22）

**何が起きたか:**
PROFIT_LOCK探索でGRIDに `ENABLE=[0,1], ARM=[15,20,30,40], USD=[5,10,15,20]` を設定。
ENABLE=0の時はARM/USDが無意味 → 16件の同一ベースライン実行が発生（32→有意義16に半減すべきだった）。

**How to apply:**
GRIDパラメータ追加前に「このパラメータがXの値の時、他のパラメータは意味を持つか？」を確認する。
意味を持たない場合はFIXED_PARAMSに移す。

---

### L-95: 既存Priority棚卸しを飛ばして新規Priority追加を提案した（2026-04-22）

**何が起きたか:**
P23が$13.50/dt-dayで限界と判断 → 「新規Priority追加が必要」と提案。
P2（231件/DT）・P3（64件/DT）・P21（0件）の棚卸しが未実施だった。
P2の理論最大は$25.85/dt-day。最大レバーを確認せずに新規追加を提案していた。

**How to apply:**
新規Priority追加提案前に、対象レジームの全Priority棚卸しを必ず実施する（Step D-1 HARD STOP）。

---

### L-96: PROFIT_LOCKがP23のadd戦略と構造的に相性が悪いことを事前に確認しなかった（2026-04-22）

**何が起きたか:**
TIME_EXIT改善としてPROFIT_LOCKグリッドサーチを実施。全16組み合わせがベースライン以下。
P23のTP_FILLED avg_MFE=$66 → ARM=$40に達する途中でPROFIT_LOCKが発動し、TP($63)到達前に$20で強制終了させていた。

**How to apply:**
PROFIT_LOCK等のExit上限系パラメータを導入する前に、TP_FILLED avg_MFEとARM_USDの位置関係を確認する。
ARM_USD < TP_FILLED avg_MFE の場合、PROFIT_LOCKはTPを阻害する。

---

### L-90: グリッドサーチ集計結果だけ見て「次のパラメータ変更」に進んだ（2026-04-20）

**何が起きたか:**
P3グリッドサーチで前半90dが全組み合わせEV<0。
すぐ「PROFIT_LOCKが問題では」という仮説を提示して次のグリッドサーチを設計した。
実際はSL_gross/TP_gross=1.875倍（損益分岐65%必要）という構造的問題と、
PROFIT_LOCK ARMがTP_grossの80%で発火し$128分の機会損失を生んでいる問題が先にあった。
これらはトレードレベル（mfe_usd/exit_reason分布/RR比）を見れば事前に発見できた。

**Why:**
- グリッドサーチの集計結果（NET/TP率）を「分析」と誤認していた
- 「なぜ負けているか」の因果分析をせずに「何を変えるか」の最適化に入った
- MFE/RR比/PROFIT_LOCK発火タイミング等の診断を省略した

**How to apply:**
- グリッドサーチ結果が出たら、次のパラメータ変更を考える前に必ずトレードレベル分析を行う
- Step 2（WORKFLOW.md）の「グリッドサーチ前必須チェックリスト」を全項目完了してからStep 3へ
- 「なぜ負けているか」を1文で言語化できるまでStep 3に進まない

---

### L-89: 新規Priority実装時に既存パラメータを「後で調整」と書いてそのまま流用した（2026-04-20）

**何が起きたか:**
P3-LONG（新規）のStep N-3 Exit設計で「MFE_STALE: P23-SHORTに準拠して後で調整」と書き、
ユーザーのGO後に gate=$4 / hold=90min をP23からコピーして実装した。
Replay結果がEV<0 → 「採用不可」と即判断した。
実際の原因はパラメータ未調整（P23向けgateをP3の異なるTP幅に流用）であり、
シグナルエッジ自体は消えていなかった。

**Why:**
- Step N-3「既存流用禁止・特性から逆算する」を形式的にしか実行しなかった
- 「後で調整」という表現でStep N-3を未完了のまま通過させた
- EV<0を見た瞬間に「採用不可」と結論したが、初回ReplayはパラメータBeforeの仮結果に過ぎない

**How to apply:**
- Step N-3でパラメータ値に「後で調整」「〇〇に準拠」と書いた時点で立ち止まる
- MFE_STALE gate は「TP gross の何%か」を必ず計算してから提示する
- 新規PriorityのEV<0 → 即不採用は禁止。グリッドサーチ完了後が採用判定のタイミング

---

### L-88: グリッドサーチは「軸数制限」でなく「値数制限」で組み合わせ爆発を防ぐ（2026-04-20）

**何が起きたか:**
TIME_EXIT改善で「軸を2〜3に絞れ」と誤解し、4×4×4=64組み合わせを設計 → 実行時間超過でキャンセル。
正しくは「軸は有効な仮説を全て入れる。値をPhase 1では各軸3点（極端値）に絞る」。

**Why:** 軸を減らすと仮説検証が不完全になる。値を絞れば3軸×3値=27で十分方向性が掴める。

**How to apply:** Phase 1は各軸[低・中・高]の3点。Phase 2はPhase 1ベスト付近の3〜5点に絞る。常に180dデータで実行する。

---

### L-87: TIME_EXIT改善は「保持時間」だけでなく5軸で考える（2026-04-20）

**何が起きたか:**
TIME_EXIT_MINを変えることが毎回最初のアプローチになっていた。
実際にはエントリー品質・ポジションサイズ・中間EXIT・MFE_STALE・保持時間の5軸がある。
今回はMFE_STALE_HOLD_MIN短縮とADX_MAX緩和の組み合わせが有効だった。

**Why:** TIME_EXITは「時間切れ」の症状だが原因は多様。保持時間を変えても根本原因が残る。

**How to apply:** WORKFLOW Step 3のTIME_EXIT改善チェックリスト5軸を必ず全て検討してから仮説を立てる。

---

### L-88: 過学習ベースラインを使うとPM計算が狂う（2026-04-20）

**何が起きたか:**
後半90d（過学習）の$43/dayをStep 0 PMチェックに使い、不足額を$17/dayと誤計算。
正しくは180d実績の$3/dayをベースラインにすべきで、不足額は$57/day。

**Why:** WORKFLOW.mdの「現在のベースライン」テーブルに後半90dのみが記載されていた。

**How to apply:** PM計算は常に180d/dayを使う。WORKFLOW.mdベースラインテーブルを180d基準に更新済み。

---

### L-86: P21 check_entry_priority はチェーン最後（7番目）。スロット競合でReplayとスタンドアロンに10倍差（2026-04-18）

**何が起きたか：**
Signal D（BB upper 上ヒゲ反落→SHORT）のスタンドアロン性能は $41/day（$3,729/90d）だったが、
Replayでは$3.6/day（$329/90d）に終わった。原因: check_entry_priority の評価順は
P4→P2→P22→P24→P23→P1→P21（最後）。P22/P23が先にSHORTスロットを取得するとP21は不実行。

**Why:** priority番号は小さいほど優先と思いがちだが、コードの実際の if-elif 評価順が優先順位を決める。
P22(22)がP21(21)より先に評価されている。

**How to apply:**
P21の新シグナルを検討する際は必ずcheck_entry_priorityの評価順を確認する。
P21優先化（P22より前に移動）はP22のEV($15/件)を奪うため、トータルNET-$5,027/90dと試算済み。禁止。

---

### L-85: P21 MACD+TRAILのrevert時は BB動的TP と TRAIL も一緒に復元する（2026-04-18）

**何が起きたか：**
P21をMACDデッドクロスにrevertした際、TRAILとBB動的TPも除外してしまい、
P21が+$649/90d→-$944/90dに崩壊した。

**Why:** P21の+$649/90dベースラインは「MACD entry + BB動的TP(BB半幅×1.0) + TRAIL(gate=0.05%)」
の組み合わせで達成されていた。P21_TP_PCT=0.005はparams内に存在するが実際には使われていない。

**How to apply:**
P21をrevertする際は必ずreplay_csv.pyで`if _entry_pri in (1, 21):`の両ブロック（TPとTRAIL）が
残っていることを確認してからReplayを走らせる。P21専用パラメータ: P21_TP_BB_RATIO=1.0,
P21_TP_MIN_PCT=0.0003, P21_MFE_GATE_PCT=0.05, P21_TRAIL_RATIO=0.8

---

### L-84: BB Mean Reversion シグナル（A/B/C/D）のExit設計比較 — E1（TRAIL gate=0.05%）が最優秀（2026-04-18）

**何が起きたか：**
7種のExit設計（TRAIL有無・TP幅・TIME_EXIT長さ）を4シグナル×ATR≥80で比較した結果、
E1（TRAIL gate=0.05% ratio=0.8）が全シグナルでNET/90d最大。

**結果サマリー：**
- Signal D(SHORT): E1=+$3,729（TRAIL率92%, TIME率1.2%）, E2(Pure TP)=+$948, E6(TP×0.5)=+$554
- TRAIL率90%超: TRAILが主要Exitとして機能。「BB動的TP×TRAILは不整合」の懸念は実測では問題なし。
- Pure TP設計（E2/E5/E6/E7）: TIME率15-30%で全シグナルNET低下。

**How to apply:**
BB Mean Reversion系のExitは TRAIL gate=0.05% ratio=0.8 を固定ベースとして使う。
TP幅・TIME_EXIT長さを変えてもE1を超えられない。

---

### L-83: ポジションサイズ調整はExit/Entry最適化が頭打ちになった後のアプローチ（2026-04-18）

**何が起きたか：**
P21/P23のTIME_EXIT削減施策（MFE_STALE・PROFIT_LOCK・MAX_ADDS・TIME_EXIT短縮）を全て試したが全滅。
ユーザーから「TP率高いPriorityのサイズを上げ・低いものを下げるバランス調整では？」と提案。
資本効率分析（$/BTC/trade）でP1($22)・P21($17)が低く、P2($140)・P23($158)・P22($78)が高いことが判明。

**Why:** Exit最適化には構造的天井がある。TP_FILLEDとTIME_EXITが同じ入り口条件から生まれると
どの出口設計も相殺されてしまう。資本効率の再配分が有効な手段として残る。

**How to apply:**
高効率Priority（P2/P22/P23）のサイズを上げ・低効率（P1/P21）のサイズを下げるバランス調整を
次セッションで検証する。P2_POSITION_SIZE_BTC が params に存在しない（LONG_POSITION_SIZE_BTC=0.024
を使用）ことを先に確認してから変更する。

---

### L-82: P23 TIME_EXIT削減はEntry/Exit両面から試みたが全て失敗（2026-04-18）

**何が起きたか：**
P23 TIME_EXIT 31件/-$908/90d の削減を以下の全施策で試みたが全て悪化または効果ゼロ：
① PROFIT_LOCK V2（lock_price→unreal修正版）: 全16パターン悪化（TP機会を削る）
② MAX_ADDS削減: P23=1〜5で5→1に下げるほど悪化（多add戦略が本質）
③ TIME_EXIT_MIN短縮: TP_FILLEDに長期保有高利益（$229/415min等）があり、短縮すると削れる
④ ATR/BB_WIDTH フィルター強化: TP/TE が同じ入り口条件で区別不可

**Why:** P23のTP_FILLEDとTIME_EXITは入り口・途中経過ともに区別が難しい設計。
TIME_EXIT 31件の avg MFE=$36 は「一時的に順行するが逆行するトレード」で、
これらを除去しようとするとTP_FILLED（avg $89/trade）も削れる。

**How to apply:**
P23は現状$11.6/dayが構造的上限。これ以上のExit/Entry最適化に時間をかけない。

---

### L-81: P21 MFE_STALE型早期カットは効果ゼロ（2026-04-18）

**何が起きたか：**
P21 TIME_EXIT 58件/-$1,096/90d に対してMFE_STALE早期カット（3d ブロック追加）を設計・グリッド実行。
HOLD=60min, GATE=$3.0でTIME_EXIT=0件になるが、NET変化は$0.24のみ（全体NET同じ）。
HOLD=45以下は正常TRAIL_EXITトレードを早期カットして大幅悪化。

**Why:** P21 TIME_EXIT 58件は「60min時点で既に今の損失水準に達している」構造。
早期カット（60min）と遅延カット（120min）で損失額がほぼ同じ = どこで切っても変わらない。

**How to apply:**
P21のTIME_EXIT対策はMFE_STALE型では解決できない。構造的残存損失として受け入れる。
追加の改善余地はポジションサイズ削減（P21の資本効率が低い）で対応する。

---

### L-80: P1/P21 が TRAIL パラメータを共有している設計バグ（2026-04-18）

**何が起きたか：**
グリッドサーチで `P1_MFE_GATE_PCT` を変えると P21 の TIME_EXIT 件数が 3倍超に変化。
P1（LONG）のパラメータが P21（SHORT）に影響する理由を調査したところ、
`replay_csv.py:760` で `params.get("P1_MFE_GATE_PCT", 0.05)` を P1/P21 共通で参照していた。

**Why:** P1/P21 の TRAIL ロジック（line 752-766）が `priority in (1, 21)` で共通処理され、
gate 値も `P1_MFE_GATE_PCT` だけしか存在しない設計。

**How to apply:**
P1/P21 を独立最適化したい場合は `P21_MFE_GATE_PCT` を新設して分離する必要がある。
現状は `P1_MFE_GATE_PCT=0.05` が最良値として確定しているため差し当たり問題なし。

---

### L-79: P1 無効化でスロット解放を期待したが全体NET悪化（2026-04-18）

**何が起きたか：**
P1 にエッジなし確認済みのため、ENABLE_P1_LONG=false で無効化を試みた。
結果、P2+6件・P4+1件しか増えず（LONGスロット解放効果ほぼゼロ）、
P1の+$215/90d を丸々失い全体 -$516/90d（-$5.8/day）悪化。

**Why:** P1 はエッジなしでも TRAIL_EXIT で +$2.4/day を稼いでいた実態があった。
「エッジなし = 無効化すべき」は誤り。実益がある限り稼働させ続けるべき。

**How to apply:**
「エッジなし」確認だけで無効化を決定しない。Replay で全体NET への影響を必ず確認してから判断する。

---

### L-78: P21_SL_PCT を Priority 別に独立化してTIME_EXIT損失を削減（2026-04-18）

**何が起きたか：**
P21 の SL が `SHORT_SL_PCT=0.05`（5%）で設定されており、TIME_EXIT（480min/-$50/trade）より
先に発動することがほぼなかった。`_calc_sl_price` に priority 引数を追加し、
`P21_SL_PCT=0.02` + `P21_TIME_EXIT_MIN=120` を採用した結果 +$186/90d（+$2.0/day）改善。

**Why:** SL=5%は120min以内では発動しない距離。TIME_EXIT短縮が主効果で、
SL=2%はセーフティネット（実際の SL_FILLED は 0件）として機能。

**How to apply:**
Priority別SLを設計する際は `P{priority}_SL_PCT` パラメータで独立管理する。
`_calc_sl_price(side, price, params, priority)` の呼び出し形式に統一済み。

---

### L-77: P21 ADX_MIN=0採用後、TIME_EXIT件数が3倍超に増加（2026-04-18）

**何が起きたか：**
P21_ADX_MIN を 30→0 に変更した結果、件数が 164→611件/90d に増加。
一方 TIME_EXIT も 7件/-$222 → 23件/-$1,159 へ急増（-$937増）。
ADX低局面（avg ADX=25）の P21-SHORT エントリーが480minまで保有されて大損。

**Why:** ADXフィルター撤廃でトレンド弱局面の逆行トレードが混入。
TRAIL_EXIT（+$1,319）が補って余りあるため全体NETは改善（+$325/90d）したが、
TIME_EXIT損失の絶対額は依然大きい改善余地。

**How to apply:**
P21_ADX_MIN=0採用後は P21_TIME_EXIT_MIN の短縮（480→120〜240min）を次タスクとする。
ADX低局面の長期保有を早期カットすることで TIME_EXIT損失を削減できる可能性がある。

---

### L-76: P21_TP_PCTはTRAIL_EXITが先行するためデッドコード（2026-04-18）

**何が起きたか：**
P21_TP_PCT を 0.005/0.010/0.015 でグリッドサーチしたが、全て同一結果。
P1_MFE_GATE_PCT=0.05%（デフォルト）でTRAILが0.05%動くと即発動するため、
TP=0.5%〜1.5%より先にTRAIL_EXITが刈り取る。TP_PCTを変えても結果に影響しない。

**Why:** P1/P21専用のTRAIL_EXITが固定TP到達を物理的に妨げる設計。
edge_checkで確認したEV=+$4.39（TP=1%/SL=2%）は、TRAIL_EXITがない場合の理論値であり、
現在の実装では達成不可能。

**How to apply:**
P21の改善でTP_PCT変更を提案する前に、TRAIL_EXITとの競合を必ず確認する。
P21の実質的な出口は TRAIL_EXIT（524件/90d）と TIME_EXIT（23件）であり、TP_PCTは無関係。

---

### L-75: P1-LONGのMACDゴールデンクロスは5m足でエッジなし・全TP/SL組み合わせでEV<0（2026-04-18）

**何が起きたか：**
p1p21_edge_check.py で MACD(12,26,9) ゴールデンクロス後の価格挙動を検証したところ、
フィルターなし・フィルターあり双方で全TP/SL組み合わせのEV<0。
TP=0.3%/SL=0.5%・フィルターあり でも EV=-$0.61、NET/90d=-$157。

**Why:** 5m足の MACD ゴールデンクロスは LONG方向の方向性エッジを持たない。
現行 TRAIL_EXIT がわずかなプラスを出しているが、それはシグナルエッジではなく
ノイズから利益を搾り取る設計になっている。

**How to apply:**
P1-LONG を改善する前に、まず「なぜゴールデンクロスがエッジを持たないのか」を検証する。
- 市場フェーズの偏り（下降相場での検証期間か）
- シグナル自体の特性（クロス後の逆行頻度）
- デッドクロスとの非対称性の原因

---

### L-74: スキャル/スイング型分類は1m足時代の残骸・5m足では廃止すべき（2026-04-18）

**何が起きたか：**
P23 の採用基準として「スキャル型 TP率≥65%」を適用しようとしたが、
P23_TP_PCT=0.7%・hold 200-340min はスキャル特性でなくスイング特性。
型を事前定義していたため採用基準が実態と乖離していた。

**Why:** 型分類（スキャル/スイング）は 1m足時代に設計されたもので、5m足移行後に実態と乖離。
WORKFLOW・CLAUDE.md に残存していた。

**How to apply:**
型は事前定義しない。検証結果（最適TP幅・hold時間・TP率）から帰納して分類する。
採用基準は「ベースライン比NET改善・全体NET改善・per-trade NET > 手数料」の3点のみ。

---

### L-73: TP_PCTをTIME_EXIT固定のまま最適化すると「広いほど常に良い」になる（2026-04-18）

**何が起きたか：**
P23_TP_PCT を [0.003〜0.030] でグリッドサーチしたところ、単調増加（0.030で最良）。
しかし TP=0.030では TP件数=8件/90d、TIME_EXIT=40件。「8件の大きな当たり」がNETを稼ぐ構造。

**Why:** TIME_EXIT=240min固定の窓内で「たまたま3%下落した取引」を拾っているだけで、
TP_PCTを広げるほど「大当たり待ち」になる。年単位・無限保持なら TP=10% でも到達するため、
このグリッドは意味のある最適化になっていない。

**How to apply:**
TP_PCTを最適化する前に必ず以下の順で設計する：
① エントリー後の価格変化分布（favorable方向にN%到達する確率）を実測する
② TIME_EXITとTPをjointで設計する（どちらか先に決めてから後を合わせると循環する）
③ 「TP × TP到達率 − TIME_EXIT損失 × TIME_EXIT率」の期待値を最大化するペアを選ぶ

---

### L-72: PROFIT_LOCK V2 はマルチadd + bar-close決済で設計バグが生じる（2026-04-18）

**何が起きたか：**
P23 PROFIT_LOCK V2: `lock_price = entry_p - ($5 / size_btc)` で、
mark_price >= lock_price のbarで close 価格を使って決済。
add=2以上だと lock_price が entry_p のすぐ近くになり（$104-$42下）、
1本のbar内でclose が lock_price を大きく超えて上昇するとSHORTとして大損失になる。

**観測:** 9件 avg MFE=$31 なのに avg net=-$10.50（avg $41ドローダウン）

**Why:** lock_price判定は「そのbarのclose」で行うため、barが高いcloseで終わると
lock_price条件が成立した時点から大幅に不利な価格で決済される。
add枚数が多いほど lock_price が entry_p に近づくため悪化。

**How to apply:**
PROFIT_LOCK系Exit実装時はexit_reason別に「avg MFE vs avg net の差」を必ず確認する。
MFE > TP_gross なのに net < 0 なら設計バグを疑う。
bar-close決済の場合は lock_price 到達barのcloseが不利になりうる設計上のリスクを事前に評価する。

---

### L-71: exit_reason別分析でMFE vs netの矛盾を最初に確認する（2026-04-18）

**何が起きたか：**
P23 exit_reason別集計でPROFIT_LOCK 9件/-$94.5 を確認していたが、
「件数が少ない」「TIME_EXITより金額が小さい」という理由で深掘りせずADX_MAX分析に進んだ。
ユーザーの指摘（「改善打ち切り判断が早くないか？」）で初めてPROFIT_LOCKの矛盾を精査した。

**Why:** TIME_EXIT（-$321）の方が損失が大きく目が行き、PROFIT_LOCKの「MFE=$31なのにnet=-$10.50」
という異常を見逃した。件数が少ない損失をスルーする習慣が根本原因。

**How to apply:**
exit_reason別分析では件数の多少にかかわらず「avg MFE >> avg net」の組み合わせがないか必ず確認する。
正常なExitは MFE ≈ net(+fee) になるはず。乖離が大きい場合は設計バグの可能性がある。

---

### L-70: P22 slope緩和はシステム全体を悪化させる（スロット競合）（2026-04-17）

**何が起きたか：**
P22_SHORT_BB_MID_SLOPE_MAX を -20 → -5 に緩和すると P22 NET は +$46 改善するが全体NETは -$85 悪化。

**Why:** P22はP23/P21より優先度が高い。slope緩和でP22の発火頻度が上がると、P23/P21のSHORTスロットを奪いより高品質なトレードを阻害する。slope=-20フィルターは「信号品質」だけでなく「スロット競合管理」としても機能していた。

**How to apply:** P22 Entry緩和を検討する際はシステム全体NETで評価する。P22単体のNET改善ではなく全体NET を採用基準にすること。

---

### L-69: 改善サイクルは Entry/Exit ロジック確定後のフェーズ（2026-04-17）

**何が起きたか：**
Entry/Exit ロジック自体の問題がある状態でパラメータグリッドサーチ（改善サイクル）に入っていた。結果、$60/day 目標に構造的に届かない施策を積み重ねていた。

**Why:** WORKFLOW の改善サイクル（Step 1-8）がパラメータ調整前提の設計になっており、「ロジック自体を疑う」フローが欠落していた。

**How to apply:** セッション開始時に「現行アプローチの最大改善幅 < 目標不足額」なら設計フェーズ（Step D-1〜）に入る。改善サイクルは Entry/Exit ロジック確定後のみ使う。

---

### L-68: セッション開始時の目標到達可能性検算を省略するな（2026-04-17）

**何が起きたか：**
「P23 TIME_EXIT DOWN_FACTOR を詰めよう」という小改善を続けていたが、損失削減の最大値($9/day)を足しても $60/day に届かないことを誰も計算していなかった。

**Why:** WORKFLOW のセッション開始プロトコルが「次のタスク」起点だった。PM視点で目標距離を先に測る手順がなかった。

**How to apply:** WORKFLOW Step 0 を毎セッション実行。「$60/day - 現在 = 不足額」→「現行施策最大 ≥ 不足額か？」→ NOなら設計フェーズへ。（Step 0 は WORKFLOW.md に追記済み）

### L-67: P4 TIME_EXIT は構造的問題でパラメータチューニングでは解決不能（2026-04-17）

**何が起きたか：**
P4 TIME_EXIT(-$613/90d, 46件)を DOWN_FACTOR短縮・延長・TRAIL_EXIT・TP_PCT変更・MAX_ADDS削減で全方向試したが全て悪化。

- DOWN_FACTOR短縮: 回復中トレードを早期カット → 悪化
- DOWN_FACTOR延長: adds 蓄積が損失拡大 → 悪化
- TRAIL_EXIT: TIME_EXIT の MFE avg 0.238% が TP(0.5%)に届かないためゲートが機能しない
- TP_PCT変更: P4 は P4_TP_PCT 未設定のため LONG_TP_PCT=0.005 が既に最適値
- MAX_ADDS削減: TP add=4-5 が avg+$42〜52/件 の高収益のため削減は逆効果

**Why:** P4 の TIME_EXIT 損失は「MAX_ADDS=5 × 損失中も add が入る設計」に起因。
TIME_EXIT 件数を減らすと TP add による利益も減る（同じエントリー条件でフィルターすると TP も消える）。

**How to apply:**
P4 TIME_EXIT 改善を試みる場合は MAX_ADDS の add interval や add 条件の変更を検討する。
単純な時間・TP・フィルター系パラメータでは解決しない。

---

### L-66: TIME_EXIT DOWN_FACTOR は Priority 別に設定すべき（2026-04-17）

**何が起きたか：**
P22 の TIME_EXIT(-$401/90d)は「SHORT_TIME_EXIT_DOWN_FACTOR=0.5 × 480min = 240分保有」が原因。
240分の長期保有中に adds が蓄積し per-trade 損失が -$40 に拡大していた。
P22_TIME_EXIT_DOWN_FACTOR=0.4（192分）に設定すると NET +$219 → +$329（+$110/90d）。

**Why:** SHORT 共通の DOWN_FACTOR を使っていたため P22 専用の最適化ができていなかった。
P22 の TIME_EXIT は「深く逆行してからの切り」だったため、短めの設定が有効。

**How to apply:**
TIME_EXIT が多い Priority があれば、まず hold_min 分布を確認する（`avg hold_min ≈ BASE × DOWN_FACTOR`）。
P{n}_TIME_EXIT_DOWN_FACTOR がなければ共通値を使っているので、グリッドサーチで個別最適化する。
replay_csv.py は `P{n}_TIME_EXIT_DOWN_FACTOR` を優先参照する実装済み。

---

### L-64: ATR14_MAXの上限設定がTP到達件数を大幅に制限していた（2026-04-16）

**何が起きたか：**
P1/P21 の ATR14_MAX=150 を設定していたため、「低ボラ環境でのみエントリー」になっていた。
BTC $84,000水準では ATR $150 は約0.18%で非常に低い。TP（0.5%）まで動かない帯でエントリーするため
TRAIL_EXIT主体（TP率8%）・per-trade $1.83 にとどまっていた。
ATR14_MAX を 9999（実質無制限）にするとTRAIL_EXIT per-trade が改善し、全体NET+$850。

**Why:** ATR14_MAX はもともと「高ボラ過多」を除外するためだが、設定値が低すぎると
「TP到達できる帯」を全部除外してしまう。TP到達時の ATR avg を確認してから設定すべき。

**How to apply:**
新規Priority設計時は、TP到達トレードの ATR avg と ATR14_MAX を必ず照合する。
ATR14_MAX < TP時ATR avg の場合、そのフィルターはTP帯を除外している可能性が高い。

---

### L-65: ADX_MIN の引き上げは TIME_EXIT 削減に最も効果的（2026-04-16）

**何が起きたか：**
P1 ADX_MIN=20 で TIME_EXIT 21件 -$948（ADX avg 26.2）。
ADX_MIN=30 に上げると TIME_EXIT 21件→7件 に削減。
「TIME_EXIT のエントリー時 ADX avg」を見れば、どこで閾値を引けばよいかが読み取れる。

**Why:** ADX が低い = 方向性が弱い = MACDクロスが擬似シグナルになりやすい。
TIME_EXIT の ADX avg ≈ 26 なら、ADX_MIN を 26〜30 に上げれば大半が除外できる。

**How to apply:**
TIME_EXIT 問題が発生したら、まず TIME_EXIT Entry指標の avgADX を確認する。
avgADX が ADX_MIN より大きく乖離していない場合は別要因を探す。

---

### L-63: standalone検証とreplay統合では SL_PCT が異なる場合がある（2026-04-16）

**何が起きたか：**
macd_edge_check.py（standalone）では `SL_PCT=0.030`（3%）で設計・検証した。
しかし replay_csv.py に統合したとき、P1/P21 は `LONG_SL_PCT=0.05`（5%）が適用されていた。
TRAIL_EXIT avg が standalone の $4.19 に対し replay で $1.83 と大きく乖離した。

**Why:** replay_csv.py の `_calc_sl_price()` は `{side}_SL_PCT` を参照する。
standalone のパラメータ設計値が replay のパラメータファイルに反映されていなかった。

**How to apply:**
standalone スクリプトで設計した SL/TP パラメータは、統合時に cat_params_v9.json の
対応するパラメータと一致しているか必ず確認する。
特に P1/P21 専用の SL_PCT が必要なら `P1_SL_PCT` / `P21_SL_PCT` として追加する。

---

### L-62: MFE_STALE のExit判定はclose価格ではなく price-level（high/low）で行う（2026-04-14）

**何が起きたか：**
`curr_pct < max_fav_pct * trail_ratio` で MFE_STALE を判定していた。
これは「closeが閾値を割り込んだ」バーでExitするため、そのバーでは high が大きく上にあっても
close が戻った時点でExitし、益が出ていない（マイナスになる）ケースが多発した。

**Why:** close価格は1本の「終値」に過ぎない。MFEをピークにしてtrailing stopを設定するなら
high/lowという「実際に価格が到達した水準」でExitすべき。

**How to apply:**
MFE_STALE / TRAIL_EXIT を実装する場合は必ず以下のように設計する：
```
# 正しい設計: price-level trailing
if fav_pct > max_fav_pct:
    max_fav_pct = fav_pct
    if max_fav_pct >= mfe_gate:
        trail_stop_price = entry * (1 + max_fav_pct/100 * trail_ratio)  # LONG

# Exit判定はhigh/lowで
if side=="LONG" and low <= trail_stop_price:
    gross = (trail_stop_price - entry) * POSITION_BTC  # 利確価格で計算
```
close価格ベースの `curr_pct` でTrailing Exitを判定することは絶対に禁止。

---

### L-61: TP幅はMFE分布を確認してから設定する（2026-04-14）

**何が起きたか：**
TP幅をBB×1.0という値に「前回の流用」で設定した。MFE分布を確認する前に値を決めてしまい、根拠のないTP幅になった。

**Why:** 「どこまで順行するか」のデータを見ずにTP幅を決めると、設計意図と乖離した値になる。

**How to apply:**
TP幅を設定する前に必ず以下の手順を踏む：

```
① 全シグナルのMFE分布を確認する
   MFE ≥ X% のトレードが何%あるかを複数閾値で計測する
   例: MFE≥0.5%=65.7%, MFE≥1.0%=42.8%, MFE≥1.5%=28.2%

② Exit設計思想からTP幅の役割を決める
   - MFE_STALEがプライマリ（近い利益を確保）なら TPは遠め（1.0〜2.0%）
     → 大きく動いた時だけTPで取る
   - TPがプライマリなら MFE分布の中央値付近（50〜65%到達率）に設定

③ 選んだTP幅で「何%のトレードが到達できるか」をMFE分布から確認してから採用する
```

「前回の流用」「なんとなく遠め」は根拠にならない。必ずMFE分布から導く。

---

### L-60: TP/SL設計フェーズの分析手順（2026-04-14）

**何が起きたか：**
ベース値（勝率37.6%・赤字）を見て「シグナルにエッジなし → シグナル変更」と即断した。実際にはTP/SL設計が原因だったが、成功/失敗トレードの動きを比較する前に結論を出してしまった。

**Why:** TP/SL設計前に「なぜTPになったか・なぜSLになったか」を分析する手順を持っていなかった。思いつきで次のアクションを提案していた。

**How to apply:**
新規設計でベース値を取った後、TP/SL設計に入る前に必ず以下の順番で分析する：

```
① TPトレードの動き
   - avg hit_bar（TP到達バー数）
   - avg MFE / avg MAE
   - MAE < SL幅の割合（一度も逆行なかった件数）

② SLトレードの動き
   - avg hit_bar（SL到達バー数）
   - avg MFE（どれだけ順行してからSLになったか）
   - 即死SL件数（MFE < SL幅×0.25）
   - 順行してからSL件数（MFE > TP幅×0.5）

③ ①②の比較からTP/SL設計の問題を特定する
   - 即死SLが多い → SL幅の問題 or エントリー品質の問題
   - 順行してからSLが多い → TP幅が遠すぎ
```

この分析なしにフィルターやTP/SL値の変更を提案しない。

---

### L-59: 目標達成アプローチは最初に数学的に検討する（2026-04-14）

**何が起きたか：**
NET $60/day 達成を目指して1mスキャルで試行錯誤を続けた。しかし最初から「0.12BTC × 手数料構造 × 必要件数」で損益分岐を計算していれば、1mスキャルが構造的に不可能であることは一発で判明した。数時間分の検証作業が無駄になった。

**Why:** シグナルやパラメータ調整に先に入ってしまい、「そもそもその設計で目標に届くか」の試算を飛ばした。

**How to apply:**
新規戦略設計の最初のステップは必ず以下の計算：
```
ポジション価値 = SIZE_BTC × 現在価格
往復手数料    = ポジション価値 × FEE_RATE × 2
損益分岐勝率  = SL_gross / (TP_gross + SL_gross) + fee調整
目標EV/trade  = NET目標/day ÷ 想定件数/day
必要勝率      = (目標EV + fee + SL_gross) / (TP_gross + SL_gross)
```
必要勝率が現実的（ランダムウォーク+10%以内）でなければ、シグナル探索より先に設計を見直す。

---

### L-58: 新規シグナル設計では「実装前にエッジを検証する」（2026-04-14）

**何が起きたか：**
P1/P21 MACDクロス戦略をcat_v9_decider.pyに実装し、replay_csv.pyで動かして赤字が出てから原因を探した。シグナルにそもそも方向性エッジがあるかを事前に確認していなかった。

**Why:** 「シグナル → 実装 → 検証」の順番で動いた。正しい順番は「シグナル → 最小コスト検証（スクリプト1本）→ エッジ確認 → 実装」。

**How to apply:**
新規シグナルは必ずWORKFLOW.md Step N-2の「シグナルエッジ検証」を実装前に行う。フィルターなし・TP/SL固定で勝率と期待値を計測し、勝率60%以上・期待値>0を確認してから実装に入る。

---

### L-57: 新規Priority追加時は「コードを開く前に」Entry特性→Exit設計を言語化する（2026-04-14）

**何が起きたか：**
P1/P21（新規スキャル）追加時に cat_v9_decider.py を先に開き、既存P2/P23のMFE_STALE+5%SL出口機構をそのまま流用した。MACDクロス（モメンタム型）に対して「外れたら即逆行する」特性を考慮せず、90分待つ出口を設計した結果、STALE損-$40/tradeが支配してNET大赤字になった。

**Why:** コードが目の前にあると「既存をどう拡張するか」の思考が自動起動し、「ゼロから設計する」発想が抑制される。

**How to apply:**
新規Priority追加時は必ずコードを開く前にWORKFLOW.mdの新規設計フローStep N-3を実行する。
「このシグナルが外れた時、価格はどう動くか？」→「その動きに合った損切りは何か？」を言語化してGOをもらってからコードに触れる。既存機構の流用は「なぜ適切か」の根拠を必ず言語化する。

---

### L-56: ユーザーの問いかけは表面の言葉だけでなく「何を指摘しようとしているか」を読む（2026-04-14）

**何が起きたか：**
「無駄フィルターがあるんじゃないか」という問いに対し、ADX/ATRフィルターの話と解釈してフィルター分析に走った。ユーザーが指摘していたのは出口機構（MFE_STALE）そのものの問題だった。誤解釈により分析・グリッド設計・WORKFLOW更新が全て無駄になった。

**Why:** 問いの表層（「フィルター」という単語）に反応し、その問いが「何を見て何を気にしているか」を考えなかった。

**How to apply:**
問いかけに対して即座に実装・分析を始めない。「この問いが指摘しようとしている構造的問題は何か」を先に考えてから応答する。

---

### L-55: cat_v9_decider.py では math が未インポート。isnan には pd.isna を使う（2026-04-14）

**何が起きたか：**
P22 RELAX PATH に `math.isnan()` を書いた。cat_v9_decider.py に math は import されていない。NameError が replay の try-except に吸収されてP22以降の全Priorityが無効化された（P23が0件になった）。

**Why:** decider.py は pandas/numpy が主体でmathは使わない。replay_csv.py の方には math が import されている（混同した）。

**How to apply:**
cat_v9_decider.py でNaN判定が必要なときは必ず `pd.isna()` または `np.isnan()` を使う。`math.isnan` は絶対に書かない。

---

### L-54: P22 RELAX PATH フィルタは PATH 1 を通った取引には効かない（2026-04-14）

**何が起きたか：**
RSI_REVERSE 5件を除外するため P22_ADX_RELAX_MIN=26 を RELAX PATH に追加した。しかし残存2件は ADX>=22 AND risk_ok=True で PATH 1 を通っており、RELAX PATH フィルタが全く届かなかった。グリッドサーチで 22〜36 全て同一結果になり発覚した。

**Why:** P22 には PATH 1（ADX+risk_ok）と PATH 2（RELAX）の2経路がある。シミュレーション時に全RSI_REV取引がPATH 2経由と誤推定した。

**How to apply:**
P22の bad trade を分析するとき、まず PATH 1 / PATH 2 どちら経由かを確認する。PATH 1 経由の bad trade は RELAX フィルタでは除外不可能。別のエントリー条件（PATH 1 への追加フィルタ）を検討する。

---

### L-53: グリッドサーチの非単調パターンは「特定 EXIT 取引の除外」が原因のことがある（2026-04-14）

**何が起きたか：**
P2_STOCH_K_MIN のグリッドで [25,30,35,40,45,50] を試したら 25が1位・45が2位という非単調結果が出た。30〜40は単調減少だが45で跳ね上がる。原因はP2 TIME_EXIT 取引（stoch_k が40〜45の間にある）が k=45で初めて除外され、TIME_EXIT損失(-$45)が回避されたため。

**Why:** フィルタ閾値が特定の悪いトレードの stoch_k 値を超えるときに NET がジャンプする。単調減少を期待すると最適値を見落とす。

**How to apply:**
グリッドサーチで非単調パターンが出たら、ジャンプした閾値の前後でどのEXIT種別が入れ替わったか確認する。TIME_EXIT や STALE が特定の閾値で除外されることでNETが跳ね上がる場合がある。

---

### L-52: PMとして目標達成ロードマップを先に作ってから作業する（2026-04-13）

**何が起きたか：**
$60/dayという目標に対して、個別Priority（P2/P23）のTP率最適化を繰り返した。結果として「TP率98%・2.4件/day」という高品質低頻度の状態になり、目標から28x離れた。ロードマップなしに目先の改善を積み上げていたため、方向性が逆だった。

**Why:** セッション開始時にWORKFLOW.mdの「現在地」だけを見て目の前のタスクを消化した。$60/dayへの到達可能性を検証しなかった。

**How to apply:**
- セッション開始時に必ず「このタスクは$60/day達成に向かっているか」を確認する
- 個別最適化の前にロードマップとの整合を確認する
- WORKFLOW.mdのロードマップセクションに従って作業する

### L-51: 最適化の目的関数を「TP率」ではなく「NET/day」に設定する（2026-04-13）

**何が起きたか：**
grid_searchの評価軸がP{N}_NETだったが、これは件数×品質の積。件数を犠牲にしてTP率だけ上げることが「最適解」に見えてしまった。TP率98%・2件/dayより、TP率85%・40件/dayの方が$60/day達成に近い。

**Why:** Priority別NETは「今のトレード数での最適」を探すだけで、「トレード数を増やしたらどうなるか」を評価しない。

**How to apply:**
- フィルター変更時は件数変化も必ず評価に含める
- 採用基準に「件数/day ≥ X」を追加する

### L-50: ATRフィルタはSTALE/TP間のATR差が≥30以上ある場合のみ有効（2026-04-13）

**何が起きたか：**
P2 MFE_STALE改善でATRフィルタを検討したが、STALE avg ATR=90.33 vs TP avg ATR=96.52 で差が6.2しかなかった。TP取引の47%がATR<95であり、ATR引き上げは大量のTP取引を除外するだけで効果なし。一方P23はSTALE avg=105 vs TP avg=137（差32）で有効だった。

**Why:** ATR差が小さいと分布が重なりすぎてフィルタにならない。

**How to apply:**
ATRフィルタ採用判定: (TP_avg_ATR - STALE_avg_ATR) ≥ 25 を目安にする。それ以下なら別の指標を探す。

### L-49: GATE_USDはTP grssより小さく設定しないとMFE_STALE判定が機能しない（2026-04-13）

**何が起きたか：**
P2 MFE_STALE_CUT条件: GATE=5.0。P2 TP gross = 0.0006 × $84,000 × 0.06 BTC = $3.02。GATEがTP grossを超えているため、add==1で120分生き残った全取引が無条件でfiredした。HOLD_MIN短縮で「TP途中の取引を誤カット」する問題が発生した根本原因。GATE=0.5に下げることで、mfe≈$0の即逆行トレードのみをカットできるようになった。

**Why:** GATE > TP_GROSS → 達成可能なmax mfeより高いため、正常取引も全てfireする。

**How to apply:**
MFE_STALE_GATE_USDを設定・変更する際は必ずTP_GROSS（size_btc × price × tp_pct）と比較する。GATE < TP_GROSS × 0.5 を目安にする。

### L-48: Priority別TP vs 損失の指標比較は全体平均ではなくPriority別に出力する（2026-04-13）

**何が起きたか：**
replay_csv.pyの「Entry指標統計 TP vs 損失」が全Priority混合の平均だったため、P2のstoch_k差（-23.62）がP23やP4の数値に埋もれて見えなかった。Priority別に分けてみてはじめて stoch_k=64.77 vs 41.15 という大きな差が発見できた。

**Why:** 全Priority混合の平均は各Priorityの特徴を打ち消す。P2/P4/P22/P23は全く異なるシグナル構造なので混合不可。

**How to apply:**
サマリー出力セクション5bに「Priority別 TP vs MFE_STALE Entry指標統計」を追加済み。分析は必ずPriority別セクションを参照する。

### L-46: 仮説はデータで根拠を確認してから提示する。平均値差だけでは根拠にならない（2026-04-12）

**何が起きたか：**
P23 MFE_STALEの原因分析でbb_mid_slope平均値に差（-19.93 vs -25.88）を見つけ、根拠確認なしに「slope不足が原因」と仮説を立てグリッドサーチを設計した。実際にデータを確認すると全12件が逆行（TPに届かなかったのではなく最初から逆方向に動いた）であり、slopeで分離もできなかった。

**Why:** 「差がある → 原因」と短絡した。「その差が損失を説明できるか」の確認を省略した。

**How to apply:**
WORKFLOW Step 3に以下を必須手順として明記済み：
① 損失 vs 成功の指標分布を比較する
② 「差がある」だけでなく「その差が損失を説明できるか」を確認する（価格実態を確認）
③ 候補パラメータ変更の効果を事前に推定する（STALE削減件数 vs TP削減件数）
④ NET改善が見込める場合のみ仮説として採用する

### L-47: 出力スキーマは「分析に必要なデータ」から逆算して設計する（2026-04-12）

**何が起きたか：**
replay_csv.pyの出力項目がmfe_usd・mae_usd・tp_diff_usdを欠いた状態で分析を開始した。分析が進むにつれて必要なデータが足りないと発覚し、都度追加することになった。旧バックテスト（CAT_v9_regime.py）ではこれらが全て揃っていた。

**Why:** 「動くものを作る」ことに集中して「何を分析するために動かすのか」から設計しなかった。

**How to apply:**
WORKFLOW Step 1の「診断インフラ確認」チェックリストに必須項目を明記済み：
- mfe_usd（最大含み益）
- mae_usd（最大含み損）
- tp_diff_usd（TPまでの距離）
- stoch_k/d_at_entry（シグナル値）
- bb_width_at_entry（ボラ幅）
新しいPriorityの分析を始める前に必ずこのチェックリストを確認する。

### L-43: 「損失フィルタ」のみのフローはスキャル定義（≥20件/day）を破壊する（2026-04-12）

**何が起きたか：**
Step 2.5の個別分析フローで「悪いトレードを削る」改善を繰り返した結果、585件/90d（6.5件/day）まで減少。スキャル定義の20件/dayを大幅に下回った。

**根本原因：**
「損失を削る」フロー → 必然的にエントリー件数が減る方向に収束する。V9はトレンド追跡型シグナルなのに、スキャルTPとの組み合わせで使っていた。構造的な矛盾を解消せずにフィルタだけ追加した。

**正しいアプローチ：**
- まず「件数≥20件/day」を達成できる構造を設計する
- Exit改善（保有時間短縮）→ Entry改善（タイミング精度向上）の順
- フィルタ追加は「件数が増える or 維持される」場合のみ採用

**How to apply:**
フロー変更後のWORKFLOW.mdに「≥20件/day未達なら採用禁止」を明記済み。採用判定の最初に件数チェックを行う。

### L-44: ワークフロー自体が設計思想から乖離しないか毎サイクル確認する（2026-04-12）

**何が起きたか：**
「Step 2.5個別分析→仮説→実装」のフローが、「V9パラメータフィルタ調整」に固定化されてしまった。フロー自体がスキャル化という目標から乖離していたが、気づくのが遅れた。

**正しいアプローチ：**
- フローは「現状の問題を解く手順」ではなく「目標に向かう汎用手順」として設計する
- 毎サイクルStep 0で「このフローで目標に近づいているか」を確認する
- 近づいていない場合は改善策ではなくフロー自体を見直す

**How to apply:**
新WORKFLOW.mdのStep 0に「継承範囲固定」と「目標達成可能性チェック」を組み込み済み。

### L-41: 5分足×スキャルTPは手数料構造上$60/dayに届かない。最初に計算すること（2026-04-10）

**何が起きたか：**
「V9をスキャル仕様にする」という方針で、TP=0.0006〜0.002の様々な値でテストを繰り返した。
何度もループした後、5分足×TP=0.0006では94件/日のTP成功が必要で5m足では不可能と判明。

**正しい事実：**
- 5m足で$60/dayを達成できる最小TP_PCT ≈ 0.0015（25件/日前提）
- TP=0.0015は「スキャル」ではなく「短期スイング」
- 5m足のグリッドサーチ（V9）でTP=0.005が最良だったのは、この構造的制約が理由
- **1m足に移行することで、スキャルTP（0.0006）×高件数（18件/日）が初めて成立した**

**Why:** 設計変更前に「この足種・TP幅・ポジションサイズで$X/dayに届くか」の試算を行わなかった。

**How to apply:**
タイムフレーム変更・TP変更を検討する前に必ず以下を計算する：
```
$60/day ÷ (TP_gross - 往復fee) = 必要TP件数/日
必要TP件数 ÷ TP率 = 必要エントリー件数/日
1日のバー数（288=5m / 1440=1m） × 現実的エントリー率（最大10%）= 上限件数
→ 必要件数 ≤ 上限件数 であることを確認してから設計を始める
```

### L-42: 設計ループを防ぐには「WORKFLOW.mdで仕様・成功条件を合意してからテスト」（2026-04-10）

このセッションで何度も設計方針が変わりトークンを大量に浪費した。
根本原因は「仕様を決める前にReplayを走らせる」「成功条件なしに結果を見て次の手を考える」の繰り返し。

**How to apply:**
1. テスト前に必ずWORKFLOW.mdに「設計仕様・成功条件・却下条件」を書く
2. 書いた内容をユーザーに確認してからReplayを走らせる
3. 結果を見て方針変更する場合は、まずWORKFLOW.mdを書き直してから実装する

### L-40: TP_PCT提案前に必ず手数料計算をする。「手数料カバー」は2つの意味を混同しない（2026-04-10）

**何が起きたか：**
TP=0.0006で「94%のTP率がある」と報告した時点で手数料計算をしなかった。
ユーザーは「手数料負けしないなら使える」と判断してスキャル設計に進んだが、
後から「手数料負け」「手数料カバーできない」と矛盾する発言を繰り返した。

**混乱の根本原因 — 2つの「手数料カバー」を混在させた：**

| 視点 | TP=0.0006の評価 | 正しいか |
|------|----------------|---------|
| 1件あたり | gross $1.20 - fee $0.56 = net $0.64 → カバーできる | ✓ 正しい |
| 戦略全体 | GROSS $133 < 手数料 $403 → カバーできない | ✓ 正しいが原因はTIME_EXIT |

**正しい事実：**
- TP=0.0006はTP1件あたり手数料をカバーできる（net $0.64）
- 戦略全体がマイナスなのはTIME_EXIT（-$588）が原因であり、手数料の問題ではない
- TP=0.0006 + TIME_EXIT撲滅 = 成立する可能性がある

**Why:** TP幅の妥当性を「TP率」だけで判断し、手数料計算を後回しにした。

**How to apply:**
1. TP_PCTを提案・採用する前に必ず以下を計算する：
   - TP gross = position_usd × TP_PCT
   - 往復fee = position_usd × FEE_RATE_MAKER × 2
   - net per TP = TP gross - 往復fee → これが正であること
2. 「戦略全体のNETがマイナス」≠「手数料負け」。TIME_EXIT/SL損失との切り分けを明確にする。
3. 手数料計算の結果をユーザーに提示してからTP幅を確定する。

### L-38: V9シグナルはTP=0.0006でも94%のTP率を出すが、add+TIME_EXITが利益を食う（2026-04-07）

V9（add有）+ TP=0.0006 でリプレイしたところ TP率94.4%を記録。シグナル自体にはエッジがある。
しかし TIME_EXIT が -$588（90日）の損失を生んで NET -$3.0/日。
add を外すとさらに悪化（MFE_STALE_CUT が増加）。
SL を 0.05→0.0006 に締めると TIME_EXIT は消えるが TP率が 68% に落ちて NET -$1.6/日。

**Why:** addシステムはTP=0.5%前提で設計されており、TP=0.0006では「逆行→add→反転でTP」のサイクルが機能しない。
**How to apply:** TP幅を大幅に変更するときは、addシステムとの整合も必ず確認する。TP率が高くてもTIME_EXIT損失が大きければ意味がない。

### L-39: 必要TP率の計算は正しいが、前提（SL手数料）の確認が必要（2026-04-07）

「7〜10%ギャップ」の計算自体は正しかった（実TP率63.6%、必要TP率73.3%）。
ただし replay_v10.py のコメントに「TP/SL ともに maker指値」とあり、SLも指値設計のため手数料計算の前提は正しい。
計算の誤りではなく、シグナルのエッジ不足が問題。

**Why:** 計算を疑う前にコードの前提（指値SL設計）を確認すれば一往復不要だった。
**How to apply:** 計算が怪しいと思ったらまず設計の前提（SL=maker/taker）を確認してから計算を疑う。

### L-35: TP幅をMFEベースで狭めすぎると手数料エッジが消える（2026-04-07）

「1本足で届く幅」を根拠にTP=$50(0.0006)に下げたが、手数料(round trip 0.028%)に対して
TP幅が小さすぎると、どのTP/SL設定でも実TP率が必要TP率を7〜10%下回り続ける。
MFE分析でTP幅を決める場合は「届きやすさ」だけでなく「手数料超過後のNET黒字」も同時に確認する。

**Why:** 「手数料負けしないTP設定をしてから調整」という最初の方針を無視してMFEだけで下げた。
**How to apply:** TP幅変更時は必ず「実TP率 ≥ 必要TP率」を確認してから採用する。
必要TP率 = (SL_gross + fee) / (TP_gross + SL_gross)

### L-36: WORKFLOW.mdの分析フローを書いた直後に自分で破った（2026-04-07）

「集計だけからの仮説立案禁止」をWORKFLOW.mdに明記した直後、MFE≤0が84.5%という
集計結果だけ見て「シグナル設計レベルで見直しが必要」と即断した。
ルールを書くことと守ることは別。書いた直後こそ意識的に適用する。

**Why:** 数字が強烈に見えると確認ステップを飛ばしたくなる。
**How to apply:** 結論を言い切る前に「この結論はStep 2のチェックリストを全て通過しているか？」と自問する。

### L-37: 分析フローなしでフィルターを試行錯誤してはいけない（2026-04-07）

RSI→Stoch OB/OS→EMA200と根拠なくフィルターを追加し続けた。
EMA200は逆張りシグナルと逆方向フィルターであり、追加するとTP率が下がった。
フィルターは「なぜ負けているか」の分析結果に基づいてのみ追加する。

**Why:** 「何か追加すれば改善するはず」という思い込みで動いた。
**How to apply:** フィルター追加前に必ずStep 2（環境分類×深掘り分析）を完了させる。

## V8移行時の教訓

### L-1: pandas/numpy 互換性
- V8移行時に Option B（adapter）で詰まった主因はライブラリバージョン不一致
- 解決策: .venv 内のバージョンを先に確認してから進める
- 現状: pandas 2.3.3 / numpy 2.4.0 / ta 0.11.0 → V9と互換性あり（確認済み）

### L-2: マーケット発注の手数料インパクト
- V8はマーケットエントリー → taker手数料が積み重なり利益を圧迫
- V9対策: 指値エントリー + 指値TP（maker）で手数料を抑制
- FEE_MARGIN=1.5 を TP計算に組み込んで手数料分を回収

### L-3: state ファイルの中途半端更新
- API失敗時に open_position.json が半端に更新されると二重EXIT等が発生
- 対策: API呼び出し後にレスポンスを確認してから state 更新（アトミックに）

## Claude との作業ルール

### L-4: ドキュメントは草案→承認→書き込みの順
- 「突然資料まとめだすのやめて」— 合意なしにファイルを作成しない
- 草案をチャットに提示 → OK をもらってから Write ツールで書き込む

### L-5: 1ターン1箇所変更
- core_rules.md にも記載: 一度に複数箇所変更しない
- 理由: デバッグ時にどの変更が原因か追えなくなる

### L-6: パラメータ値は必ず一次ソースで確認
- validate_archive.py の値（分析用）と CAT_v9_regime.py 本体の値が異なる場合がある
- 正本: `strategies/CAT_v9_regime.py` の params dict（2829-2935行付近）

### L-7: S-7待機中の runner STOP は「TP/SL約定」が第一候補
- S-7テスト待機中（EXIT発火待ち）に runner が STOP した場合、まず「TP/SL約定でポジションが消えた」可能性を疑う
- `tp_order_missing` + ポジション消滅 = S-7シナリオ（エラーではない）
- **Why:** WORKFLOW.mdに次タスクが明記されていても、STOPログを見た瞬間にエラーと誤判断してしまった
- **How to apply:** runnerがSTOPしたら、まずWORKFLOW.mdの「次のタスク」と照合してから原因を判断する

### L-9: Phase 2テストは手動1回実行。autoループはテスト中禁止

- `while true` autoループはPhase 5（常時稼働）用。Phase 2テスト中は使わない
- テスト中は `.venv/bin/python3 runner/run_once_v9.py` を1回ずつ手動実行する
- 各runの前にClaudeが「何が起きるか・成功条件」を提示 → GOをもらってから実行
- autoループ中はTPやSL約定など外部イベントが予告なく発生し、テスト状態が崩れる

**Why:** autoループ中にTP約定でポジションが消え、S-8テストの機会を失った。
overrideバグとの組み合わせで意図しない2回目発注も発生した。
**How to apply:** Phase 2テストでrunを実行するたびに必ずGOを得る。
1回のrunで何が起きるかを事前に明示してからBashを叩く。

### L-10: Bitget hedge_mode の能動クローズは close-positions エンドポイントを使う

- `place-order + tradeSide: "close"` は one-way mode 専用。hedge_mode では **22002 "暂无仓位可平"** エラーになる
- 正しいエンドポイント: POST `/api/v2/mix/order/close-positions`
  - パラメータ: `symbol` / `productType` / `holdSide`（"long"/"short"）
  - SDK: `self.api.closePositions({...})`（bitget/v2/mix/order_api.py L25）
- **Why:** `tradeSide: "close"` は get_single_position レスポンスにも存在するフィールド名で、
  place-order のリクエストパラメータとして使えると誤解しやすい
- **How to apply:** close 処理を変更・確認するときは `closePositions` エンドポイントを使っているか確認する

### L-8: 実行コマンドは `.venv/bin/python3` を使い、冒頭に RUN START を入れる
- システムの `python3` には `requests` 等がインストールされていない → 即終了・無音で失敗する
- cron・whileループ・手動実行コマンドを提示する際は必ず `.venv/bin/python3` を使う
- 実行コマンドの冒頭には必ず以下を付ける（CLAUDE.md MUST項目）:
  ```
  echo "=====🚀  RUN START $(date) =====" && .venv/bin/python3 runner/run_once_v9.py
  ```
- **Why:** `python3` で無音失敗した際、ユーザーが何も出ないと報告するまで気づけなかった。RUN STARTがあれば即座にPython到達前の失敗と判別できる

### L-11: 特定・実装・記録の分離による抜け漏れ防止

**再発防止フロー（以下の順を必ず守る）:**

1. **特定したらすぐ書く** — ギャップ・修正案・未確認項目を発見した瞬間に
   `project_v9_progress.md` の未完了リストへ記録する。実装より先。
2. **1トピック完結してから次へ** — 「特定→実装→チェックリスト追記→文書更新」が
   揃ってから次のトピックに移る。途中で別トピックに引っ張られない。
3. **「次に進みますか？」の前に確認** — 次トピックを提案する前に、
   今のトピックで特定した全項目が未完了リストまたはチェックリストに記載済みかを確認する。
4. **セッション終了前の読み合わせ** — `project_v9_progress.md` 未完了リストを
   声に出して読み、会話中に出た項目と照合する。漏れがあれば追記してから終了する。

**Why:** 複数トピックを並走させると、特定済みだが未記録の項目が会話の流れで消える。
今セッションでは hedge_mode 固有テスト項目（5件）を特定後に別実装に移ったため未追加になった。

**How to apply:** 何かを「発見・特定」した時点でそれをトリガーとして
project_v9_progress.md を開き、未完了リストに書いてから実装に入る。

### L-12: TTLキャンセル後の PARTIAL_FILL_TP_SET 誤発火

ADDのpendingがTTL切れキャンセルされた際、既存ポジションがあると
`remaining_sz > 0` が True になり PARTIAL_FILL_TP_SET が誤発火する。

修正: `remaining_sz > existing_sz`（ADD前のポジションサイズを基準にする）
- `open_pos is None` → 新規ENTRYの部分約定 → TP設定が必要
- `remaining_sz > open_pos["size_btc"]` → ADDの部分約定 → TP再設定が必要
- `remaining_sz == open_pos["size_btc"]` → ADDは未約定 → 何もしない

**Why:** TTLキャンセル後のポジション残存チェックが「増加分」でなく「存在有無」だった。
**How to apply:** S-1③テスト時に必ず PARTIAL_FILL_TP_SET が出ないことを確認する。

### L-13: SL_PCT が小さすぎると 40917 エラーで confirm_entry STOPループになる

SL_PCT が極小（例: 0.0003）の場合、ADD約定後〜SL設定の数秒間に
価格がSL圏内まで下落すると Bitget が 40917 "SL価格 < mark_price" を返す。
このとき `_confirm_entry` は STOP するが `pending_entry.json` が未クリアのまま残り、
次回 run でも同じフローを繰り返す無限 STOP ループになる。

**Why:** SL_PCT=0.0003 でテストしたところ、ADD約定後に価格がSL圏内まで急落して発生。
本番では SL_PCT=0.05 のため通常起きないが、極端な急落（5%超/数秒）時には可能性あり。
**How to apply:**
- `_place_sl` で 40917 を検知したら即 closePositions を呼ぶ（✅ 修正済み 2026-03-23）
- STOP 前に pending_entry.json を必ずクリアする（✅ 修正済み 2026-03-23）
- SL_PCT テスト値は 0.001 以上を目安にする（0.0003 は小さすぎる）

**修正内容（2026-03-23）:**
1. `filled` + `_confirm_entry` 例外時に `_PENDING_PATH.unlink()` を except ブロック先頭に追加（STOPループ防止）
2. `_place_sl` が 40917 を受け取ると `RuntimeError("SL_PRICE_INVALID:40917: ...")` を raise
3. 呼び出し元 except ブロックで `"SL_PRICE_INVALID:40917"` を検知 → `_do_close` で即クローズ → `EXIT_COMPLETE(exit_reason=SL_PRICE_INVALID)` ログ

### L-14: Bitget デモ口座では SL plan order が「消滅するだけでポジションを閉じない」バグがある

デモ口座で SL が発動価格を超えた際、plan order が entrustedList から消えるが
ポジション（total > 0）はそのまま残存する。本番では再現しない可能性が高い。

**観測内容（2026-03-23 セッション20）:**
- SL plan order が mark_price ~68627 で entrustedList から消滅（planStatus="executed" に移行）
- しかし exchange の position.total = 0.0719 のまま残存
- exchange レスポンス: `stopLoss=""`, `stopLossId=""` → SL設定が消えている
- fill-history は `fillList: null` → close 約定なし（実際に決済されていない）

**Why:** Bitget デモ環境固有の制限と推定。本番 API との挙動差異。
plan order の「消滅」= 約定証拠とするのは危険（デモでは偽陽性になる）。

**How to apply:**
- SL_FILLED の証拠は plan history の消滅ではなく fill-history の close 約定のみとする（Change A）
- SL 消滅 + ポジション残存 = 異常状態 → `STOP(sl_order_missing_pos_exists)`（Change B）
- デモで SL_FILLED の自動確定テスト（S-7②）は不可。本番でのみ確認する
- fill-history の `fillList: null` はデモ口座では正常レスポンス（エラーではない）

### L-16: 改善は「集計分析→個別分析→仮説→検証」の順で行う（当てずっぽう禁止）

パラメータを直感で変えてReplayを回すと悪化することが多い。必ず以下の順を守る：
1. 集計分析（Priority別・Exit reason別）
2. 個別分析（勝ちvs負けのエントリー時指標分布を比較）
3. 差が大きい指標を根拠に仮説を立てる
4. 変更件数を事前に計算して提示する
5. Replay で検証

**Why:** TIME_EXIT の早期カット試行など、根拠なしの変更が連続して悪化した。
**How to apply:** Step 2.5（個別分析）なしに Step 3（改善案提示）に進まない。

### L-18: 1m足テクニカルシグナルはランダムウォーク理論値を下回る

どのシグナル（mean_reversion / trend_follow / ema20 / rsi9 / ADXレジーム / Stoch+RSI）を使っても
1m足でTP率64〜65%に収束した。TP=0.15%,SL=0.30%のランダムウォーク理論値は66.7%なので
**全シグナルがランダム以下**だった。

**Why:** 1m足BTC先物はノイズが支配的で、短期テクニカルシグナルがランダムに勝てない。

**How to apply:**
- 1m足でシグナルを変えても無意味。まず5m足以上で検証する。
- TP/SL設定変更前に「ランダムウォーク理論値 = SL/(TP+SL)」を計算して基準にする。

### L-19: 5m足Stoch+RSIで初めてランダムを上回った（GROSS黒字）

5m足 + Stoch K/Dクロス + RSI(9)<30 + TP=0.15%,SL=0.30% の設定で：
- TP率 67.7%（ランダム理論値 66.7%を上回る）
- GROSS +$205（初のプラス）
- NET -$5.56/日（手数料$1,211が障壁）

損益分岐は72.8%。あと5.1%TP率を上げれば黒字。

**Why:** 5m足は1m足よりノイズが少なく、Stoch+RSIが有効に機能し始めた。

**How to apply:**
- 5m足を基本時間足として開発を継続する。
- 次のアプローチ: RSI閾値を厳しくする（30→25or20）でTP率向上を狙う。

### L-20: TIME_EXITはtaker手数料+逆行損の二重パンチで赤字の主因

TIME_EXIT廃止後にGROSSが改善。逆行したまま保有し続けてtaker手数料で成行決済は
二重に不利（含み損+taker手数料）。TP/SLのみで決着させるべき。

**How to apply:** TIME_EXITは実装しない。TP/SL決着のみ。

### L-17: P2 LONG は ADX < 25 での発火が損失の主体

ADX 15-20帯（218件）が -$1,098 の損失源。ADX >= 25 フィルターで +$608 改善を確認（2026-04-03）。
フィルター追加後の P2 NET: -$1,392 → -$784。

**Why:** トレンドが弱い局面でのストキャスゴールデンクロスはダマシが多い。
**How to apply:** P2 ADX_MIN は 25.0 で設定済み。他の Priority でも ADX分布を先に確認する。

### L-18: cat_v9_decider.py の RSI カラム名は rsi_short

- `get("rsi")` ではなく `get("rsi_short")` が正しいカラム名
- `get("rsi")` は存在しないカラム → `nan` → `nan <= 60.0` = `False` → 全件除外
- **Why:** P4_RSI_MAXフィルター追加時に `get("rsi")` と書いてP4が0件になった
- **How to apply:** cat_v9_decider.py でRSIを参照する際は `rsi_short` を使う。
  他の指標も正しいカラム名を事前に確認してから書く（replay_csv.py L541付近参照）

### L-19: Priority間の相互作用を必ず考慮する（フィルター追加前にチェック順を確認）

P4フィルターを追加すると、除外トレードが P2 に流れ込む（decider のチェック順: P2→P22→P24→P23→P4 ではなく P4が途中に入るため）。単一Priority での試算が他Priority への流入で相殺・逆転することがある。

**失敗例（再現2件）:**
- P4_RSI_MAX=55: P4 +$103 改善 → P2 -$167 悪化、トータル -$65（2026-04-03）
- P4_RET5_MAX=0.25: P4 ±0改善（+$91→+$91） → P2 -$47 悪化（+$138→+$91）、トータル +$76→+$29（2026-04-04）

**CSVシミュレーションの限界:** CSV上では P4 単独 +$34 改善と計算されるが、Replay では P4 を除外したバーで P2 が代わりに発火するため実際の改善は発生しない。CSVシミュレーションは同一バーの Priority 流入を考慮しない。

**Why:** P4 と P2 はどちらも LONG。同じバーで P4 が弾かれると P2 条件を満たすケースがある。
**How to apply:**
- P4単独フィルターを追加する前に「同バーでP2が発火する可能性があるか」を cat_v9_decider.py のコードフローで確認する
- P4フィルターは P2 と同時適用（ret_5 > 0.25 なら P2 も除外）しないと L-19 を防げない
- Priority 間に重複するエントリー条件がある場合は特に注意

**失敗例（再現3件目）:**
- P4_ADX_MAX=30（ADX>=30 P4 除外）: P4 +$77 改善 → P2 -$318 悪化（12件流入・avg -$26/件）、トータル -$241（2026-04-05）
- L-19 はシミュレーション(+$123) を Replay(-$241) が大きく下回る形で発動。P2 が ADX>=30 スロットを埋めた。
- **打開策:** P4_ADX_MAX + P2_ADX_MAX を同時設定してL-19 フローを塞ぐ（同条件を両方弾く）か、エントリーフィルターでなくエグジットロジックを変える。

### L-20: TIME_EXIT_MIN 短縮は再エントリー増と早期TP機会損失を招く

P2_TIME_EXIT_MIN 480→200 にしたところ TIME_EXIT 件数が +39件増、TP件数 -16件減でトータル悪化（-$80）。早期カットが「次のエントリー機会」を生み件数が膨らむ。また一部TP到達できたはずのトレードも失う。

**Why:** 「早くカットすれば損失が浅くなる」という直感が正しくない。相場が一時逆行後に回復してTPに届くパターンを潰してしまう（L-15 と同じ構造）。
**How to apply:** TIME_EXIT_MIN 変更は件数変動・TP件数変動も含めて評価する。試算前に「カットされたトレードがTPに届いていたか」をデータで確認してから変更する。

### L-15: P23 SHORT MIDTERM_CUT は TIME_EXIT より早い段階で損失を固定しやすい

hold=240min 時点でカットすると、360min TIME_EXIT より大きい損失になるケースが多い。
相場が一時的に逆行して最悪値を付け、その後部分回収するパターンに干渉してしまう。

**観測内容（2026-04-03）:**
- P23 SHORT MIDTERM_CUT（hold≥240min, unreal<-$25）を replay_csv.py に追加してテスト
- 結果: NET -$146.60 → -$187.96（悪化）
- TIME_EXIT 16件 -$529 → 10件 -$158（改善）だが MIDTERM_CUT 7件 -$449 が新たに発生
- 例: 240min 時点 -$50 だが 360min には -$27 まで回収したケース → MIDTERM_CUT が損失を固定

**Why:** TIME_EXIT の「360min時点の損失」は一時的な最悪値を過ぎた後の価格を見ている。
MIDTERM_CUT で早期カットすると最悪値付近で決済するリスクが高い。

**How to apply:**
- P23 SHORT の損失削減は「カットタイミング調整（MIDTERM_CUT）」よりも
  「エントリー制御」または「ADD 上限削減」で攻める
- MIDTERM_CUT を使うなら TIME_EXIT 直前（≥310min）かつ深い損失（<-$50）の
  閾値が最低ライン。それ以外は逆効果になる可能性が高い

### L-21: Replay の SL シミュレーション欠陥（SL 縮小時に露呈）

SL=5% 時代は add_count=1 の `sl_price=None` でも実害なし（SL 到達がほぼゼロ）。
SL を縮小すると2つの欠陥が露呈した:
1. add_count=1 の新規エントリーで `sl_price=None`（SL が設定されない）
2. TP/SL 判定が close ベース（high/low ではない）→ 狭い幅で検知漏れ

**修正済み（2026-04-03）:**
- `sl_price`: 常に `_calc_sl_price()` でセット（add_count=1 でも）
- TP 判定: `high_p >= tp`（LONG）/ `low_p <= tp`（SHORT）
- SL 判定: `low_p <= sl`（LONG）/ `high_p >= sl`（SHORT）

**Why:** SL=5% では修正前でも誤差が無視できたため、SL 縮小検証まで気づけなかった。
**How to apply:** SL 幅を変更する前に Replay の SL シミュレーション精度を確認する。

### L-22: シグナルの edge 検証は「Replay エントリー固定 → bar-by-bar グリッドサーチ」

1. Replay を走らせてエントリーポイントを確定（entry_time / entry_price / side）
2. そこから bar-by-bar で TP/SL 先着を計算（ohlcv の high/low を順に追う）
3. 各 TP%/SL% 組み合わせで TP 到達率 vs ランダムウォーク理論値を比較
   - 理論値: `SL_pct / (TP_pct + SL_pct)`
   - 実測が理論値を +5% 以上上回れば edge あり

全 Priority でランダムウォーク理論値を **+10〜23% 上回る edge を確認**（2026-04-03）。

**Why:** Replay 実走は出口ロジックが絡んで「シグナルの純粋な edge」が見えにくい。
**How to apply:** 出口設計を変える前に、まずこの方法で edge の有無・大きさを確認する。

### L-23: add=1 制限はシグナルの edge を殺す

ADD の本来の効果: 逆行時に平均エントリーを改善 → TP ヒット率が上がる設計。
add=1 に制限すると「即 TP or 即 SL」設計になり、ATR より狭い SL 幅では連続被弾。

2026-04-03 セッションでは add=1 のまま TP/SL 設計を変えようとしたが、
エントリー頻度が 3.5件/day に低下し EV 上限が ~$21/day になった。

**Why:** add 制限はフィルタリング改善の一環として導入したが、ADD 本来の役割（逆行吸収）を失わせる。
**How to apply:** 次セッションでは add=3 を復活させてから TP/SL を設計する。

### L-25: post_only maker 指値のfill rate は33%しかない（モメンタム系シグナルと相性が最悪）

ENTRY_SEND 278件 → ENTRY_CONFIRMED 92件 = fill rate 33%（2026-04-03 ライブログ確認）。

**Why:** post_only 指値は close ±0.01% に置かれる。LONG シグナルはモメンタム上昇時に発火するため、
価格はさらに上昇して指値には戻ってこない。Replay は全約定前提なので 3× 過大評価になっていた。
パラメータをいくらチューニングしても fill rate 問題が解決されない限り実効 NET は改善しない。

**How to apply:**
- エントリーを taker（成行 or aggressive limit）に変更する（run_once_v9.py + replay_csv.py 両方）
- Replay でも taker 想定（= close 価格で即約定）に統一してから TP/SL を最適化する
- fill rate < 50% の状態でパラメータチューニングをしない

### L-26: Entry フィルターのバーズレ問題（signal バー vs fill バー）

CSV シミュレーションと Replay で `ret_5`/`atr_14` の参照バーがズレる。

- CSV シミュレーション: fill バー(i+1)の値で判定（_calc_entry_states）
- check_entry_priority フィルター: signal バー(i)の値で判定

特に `ret_5` のような短期リターン系は1バーで大きく変わるため、
シミュレーション +$143 改善 → Replay 実質 +$2 という乖離が発生（2026-04-04）。

**Why:** Replay は「シグナル発火後に約定を試みる」設計のため、約定が確定するのは
シグナルバーの次のバー以降。シグナル時点のモメンタム指標は約定時には陳腐化している。

**失敗例（再現2件）:**
- P23 transit_A × ret_5>0 フィルター: CSV +$143 → Replay +$2（2026-04-04）
- P23 ret_5 ≤ 0.05 フィルター: CSV期待値 +$242 → Replay +$6。TP 40件が余計に除外（2026-04-05）

**結論: `ret_5` を Entry フィルターに使うことは禁止。** CSV と Replay で件数が大きく乖離するため検証不能。

**How to apply:**
- `ret_5` を Entry フィルターとして提案しない（CSV シミュレーション値が信頼できない）
- Exit タイミング変更（TIME_EXIT_MIN 短縮）はバーズレがなく、Entry フィルターより CSV 精度が高い（ただし L-20 も参照）
- Entry フィルターに使える指標は atr_14（14本平均で変化が緩やか）のみ実績あり

### L-27: ~~Replay CSV の entry_time / exit_time は JST、candles.csv は UTC~~（2026-04-05 根絶済み）

**修正済み**: `replay_csv.py` の `_ts_to_str` を `tz=timezone.utc` に変更。
entry_time / exit_time は UTC で出力されるようになった（candles.csv と一致）。
entry_hour は引き続き JST（時間帯分析用）。MFEマッチ率 54% → 100% に改善。

**観測（2026-04-05）:**
- P23 entry_time "2026-01-07 11:20:00"（JST）で candles を検索 → 実際は UTC の 11:20バー（JST 20:20）を参照
- 「届かずグループ8件・MFE>1%」と誤判断 → UTC修正後は届かず0件・全件が逆行グループ
- 逆行グループの誤った MFE（CSVの low が entry_price より大幅低い別日のバー）が「TP到達」と見えた

**Why:** replay_csv.py が JST タイムスタンプを出力するが、candles CSV は UTC。この差を考慮しないと 9 時間ずれたバーで MFE/追跡計算をしてしまう。

**How to apply:**
- 分析スクリプトで candles を参照する際は `entry_time_utc = entry_time_jst - pd.Timedelta(hours=9)` に変換する
- MFE計算・延長追跡・バー単位確認は必ず UTC 時刻で行う
- Replay CSV のタイムスタンプを candles に当てる前に「entry_price が entry_time バーの high/low 範囲内か」を確認してタイムゾーンを検証する

### L-24: 現設計の理論的な収益上限

```
エントリー頻度 × EV/trade = 日次収益
3.5件/day × $6/trade = $21/day （add=1, TP+2%/SL-1% 設計）
```

$120/day 達成には以下のいずれかが必要:
1. **ポジションサイズ拡大**: 0.024 → 0.16 BTC（資金・リスク増）
2. **エントリー頻度増加**: add 復活・Priority 条件緩和
3. **EV/trade 向上**: add による平均改善 or より精度の高いシグナル

add=3 復活はエントリー頻度・EV/trade 両方に効く可能性がある。

**Why:** add=1 制限 + narrow SL の組み合わせで設計的な収益上限が $21/day に低下した。
**How to apply:** 設計変更前に「件数 × EV/trade の天井」を試算してから進む。

### L-28: P22 TIME_EXIT短縮のシミュレーション限界（hold_min分析の欠陥）

**失敗内容（2026-04-05）:** P22のTP hold_min≤180minを「180min短縮でもTP safe」と判定したが、Replayで悪化（-$112）。

**Why:** hold_min分析は「最終的な保有時間」しか見えない。「途中（180min時点）でunreal<0だが、その後TPに到達する」トレードの存在を見落とす。P22 TP hold_min=180min近辺の件数が多く、このゾーンで意図しないカットが発生した。

**How to apply:** P22（SHORT干渉が強い）でのTIME_EXIT変更は孤立シミュレーション不正確に加え、hold_min分析自体も不正確。P22のExit変更はReplayのみで判断し、シミュレーション期待値を信頼しない。

---

## V10 開発の失敗教訓（2026-04-06〜）

### L-30: Bitget の 1m 足は約31日分しか取得できない

Bitget API で `granularity=1m` を指定すると、約31日以前は `data: []` を返す。
180日分リクエストしても31日しか取れない。

**How to apply:** 1m 足の長期データは Binance の公開 API（`fapi.binance.com/fapi/v1/klines`）を使う。
認証不要・1リクエスト1500本・180日分が約2分で取得可能（`tools/fetch_binance_1m.py`）。

### L-31: ストキャスのみのエントリーはコイントスに等しい（TP率47%）

フィルターなしのストキャスクロスは 90件/日 以上出るが、
エントリー後の短期方向性はほぼランダム（TP率47%）。
1:1 RR で損益分岐50%に届かず、手数料込みで必ず赤字。

**How to apply:** ストキャス単独でのシグナル設計は禁止。必ず方向性フィルターと組み合わせる。

### L-32: 重い指標（200EMA + BB タッチ）の同時成立は件数を激減させる

`close > 200EMA` + `close <= BB下限(-2σ)` + `Stoch クロスアップ` の3条件同時成立は
1分足では非常に稀（最終的に **1.7件/日** まで低下）。
BB_ENTRY_STD を 2.0σ → 1.5σ → 1.0σ と緩めても TP率は 46〜47% で変わらず、
件数が増えるだけで収益性は改善しない。

**Why:** Stoch が OB/OS に達するタイミングと BB 外側タッチが同時に起きることが本質的に稀。

**How to apply:**
- 1m 足には「軽くて反応が速い」フィルターを使う（20EMA・RSI9 等）
- 200EMA + BB の組み合わせは 1m には重すぎる。5m 以上の足向け
- 件数が 10件/日 を下回ったら条件が厳しすぎるサイン → 組み合わせ自体を見直す

### L-33: どのフィルターを加えても TP率42〜45% から動かない

20EMA / RSI9 / 200EMA+BB / trend_follow / mean_reversion と様々なフィルターを試したが、
TP率は **42〜45% で収束**。損益分岐（1:1 RR で50%）に届くフィルターが見つかっていない。

| MODE | TP率 | NET/日 |
|------|------|--------|
| ema20 | 42% | -$18.9 |
| rsi9 | 42% | -$8.1 |
| mean_reversion | 45% | -$5.0 |
| trend_follow | 23% | -$41.0 |

**Why:** 1m 足のランダムウォーク性が強く、短期方向性をフィルターで改善する余地が小さい。

**How to apply:**
- 新しいフィルターを試す前に L-22 の方法でシグナルの edge を先に確認する
- TP率改善が難しければ TP/SL の非対称設計（TP を広げて SL を狭く）で RR 比で補う

### L-34: SL なし（TIME_EXIT のみ）は逆行した損失を長時間抱えるため悪化する

SL を廃止すると TIME_EXIT avg が **-$4.15/件** になった（SLあり時は -$1.27）。
逆行したポジションを最大20分保有し続けるため損失が膨らむ。

**How to apply:**
- SL は必ず設定する。SL なし設計は 1m スキャルでは機能しない
- 「TP か SL のどちらかで決着・TIME_EXIT なし」の設計を先に検証すること

### L-29: スキャル化（TP縮小＋SL縮小）はadd構造と根本的に競合する

**失敗内容（2026-04-05）:** TP=0.003, SL=0.02でReplay実走 → NET $1,828→$464（-$1,364悪化）。SL_FILLED 2件→15件（avg -$53/件）。

**Why:** addが積み上がるほど同じSL幅での損失が倍増する（add=5でSL=2%発動 → size=0.12BTC × 2% × 65000 = $156/件）。スキャル型SLとadd構造は根本的に非互換。

**How to apply:** TP幅縮小とSL幅縮小を同時にやるときは「MAX_ADDSを0か1に制限」しないと設計崩壊する。add構造を維持するなら現行SL=5%は正当な設計（addの砦として機能）。

---

### L-42: 禁止提案を毎回繰り返す根本原因と対策（2026-04-11）

**何が起きたか：**
同一セッション内で以下の禁止提案を複数回繰り返した。
- 「5m足に戻す」→ CLAUDE.md 提案禁止リスト明記済み（V9回帰）
- 「TP=0.005」→ CLAUDE.md 提案禁止リスト明記済み
- 「TP_PCT引き上げ」→ スキャル設計に反するとユーザーが明示済み
- 「add=1でまずプラスに」という指示を無視してMAX_ADDS=2を先に実行

**根本原因：**
1. 提案を出す前にCLAUDE.mdの禁止リストを参照するステップを毎回省略している
2. ユーザーの指示を「一時的なコメント」として処理し、以降の判断に反映していない
3. 「問題を発見すると即座に解決策を生成する」癖があり、ルール確認より先に提案が出る

**How to apply:**
提案を出す前に必ず以下を確認する（毎回・例外なし）：
1. CLAUDE.md「提案禁止リスト」に該当しないか
2. 直近のユーザー指示と矛盾しないか
3. 設計思想（スキャル型・1m足・小TP）と整合しているか

この確認を省略した提案は出さない。

### L-45: 改善提案前に「考慮すべき全要素」を宣言してから分析する（2026-04-12）

**何が起きたか：**
P4 TIME_EXIT改善で「時間短縮→損失削減」と単純に仮説を立ててGO→失敗。分析前に以下を確認していなかった：
- MFE（トレードが一度でもプラスになったか）
- post-exit TP到達（切った後にTPが来たか）
- スロット占有コスト（TIME_EXIT保有中に他シグナルが取れなかったか）
- 損失の時系列（exit後に悪化したか、それとも回復したか）

**分析結果（2026-04-12 P4 TIME_EXIT 10件）：**
- 6/10件: exit後にTPを達成（切るのが早すぎた）
- 全件: MFE > 0（avg $0.40）→ エントリー自体は正しいが直後に一時逆行
- スロット占有コスト = ゼロ（TIME_EXIT中にLONGシグナル発火なし）
- 4/10件: exit後さらに$8〜$27悪化（エントリー品質の問題）

→ TIME_EXITを「時間短縮」で改善しようとしたのは完全に誤り。6/10は良いエントリーで時間が必要だっただけ。4/10はエントリー品質の問題でEntryフィルタで対処すべき。

**正しい分析プロトコル（EXIT改善提案の前に必ず実施）：**

```
[Exit改善前チェックリスト]
□ 1. MFE確認: トレードは一度でもプラスになったか？
     → MFE=0なら方向失敗（エントリー問題）
     → MFE>0なら出口タイミング問題の可能性

□ 2. post-exit TP到達: exit後X分以内にTPを達成したか？
     → 多数達成 → 「もっと保有すべき」→ TIME_EXIT延長 or exit条件緩和
     → 少数達成 → 「切る方向は正しい」→ 早期exit検討

□ 3. post-exit 悪化: exit後に損失が拡大したか？
     → 悪化 → 「切るのは正解、タイミングの問題」
     → 回復 → 「切るのが早すぎた」

□ 4. スロット占有コスト: TIME_EXIT保有中に他シグナルが発火したか？
     → 発火あり → スロット解放に価値あり（早期exit有効）
     → 発火なし → 時間短縮のメリットなし（今回はゼロ）

□ 5. 損失の均一性: TIME_EXIT lossersは「境界線エントリー」か「方向失敗」か？
     → avgRSI/ADX/Slopeが閾値ギリギリ → Entry filter改善が正解
     → 分布が広い → Exit条件の問題
```

**How to apply:**
- EXIT改善を提案する前に上記5項目をCSVで確認・宣言してから仮説を立てる
- 「TIME_EXIT件数が多い」だけで「時間短縮すれば改善」と結論しない

---

### L-46: ポジションサイズ提案前にスロット占有コストを計算しない（2026-04-23）

**何が起きたか:**
PMレビューでP21サイズ倍増（0.06→0.12 BTC）を提案した。
P21保有中（TIME_EXIT_MIN=120min）は他の全Priorityがエントリー不可（シリアル実行）のため、
P23（+$18.52/dt-day）をブロックする機会損失がサイズ増加利益を上回る可能性が高い。

**根本原因:**
`pm_review_template.md` の呼び出し手順に「スロット占有制約（シリアル実行）」の記載がなく、
PMブリーフィングにこの制約を含めなかった。L-45の教訓がPMプロセスに未接続だった。

**Why:** V9はシリアル実行（1ポジション保有中は他Priority入れない）が前提。
この制約を渡さないとサブエージェントは「独立した複数ポジションが同時保有可能」と誤解して
サイズ増加 = 純利益増として計算してしまう。

**How to apply:**
ポジションサイズ変更を提案する前に必ず以下を計算し、数値で示してから提案する:

```
機会損失 = avg_hold_min ÷ 5min × 他Priority件数/day ÷ 288 × 他Priority EV/trade
サイズ増加利益 > 機会損失 → 提案可
サイズ増加利益 ≤ 機会損失 → 提案禁止
```

PMブリーフィング作成時は「スロット占有制約（シリアル実行）」を不変制約として必ず明記する。

---

### L-47: P2 TP=0.005 は仮想シムで壊滅的と確認（2026-04-23）

**何が起きたか：**
TP=0.005 禁止ルールがV10スキャル時代の古いルールか確認した結果、ルール自体は削除されたが、
仮想シミュレーションで現在のP2設計（TP=0.004）に対しTP=0.005は壊滅的に悪いことが判明。

**数値（365d OOS・P2 202件）:**
- TP_FILLED 117件のうち、85件が新TP未到達 → $1,100利益消滅 → $-828損失へ転落
- MFE_STALE/TIME_EXIT: 新TP到達ゼロ（MFE中央値 $0.9〜$5）
- 推定NET: +$301 → -$1,533（-$12.83/dt-day悪化）

**根本原因：**
現在のP2は「低MFEで小さく勝つ × 多件数」構造。TP=0.005はこの構造と相性が最悪。
旧era の良い成績（$+1,270/170件）は ADX_BOOST・レジームフィルターなし・MFE_STALE_CUTなし等の複合条件であり現在と別物。

**How to apply:**
P2の TP_PCT = 0.004 は変更禁止。仮想シムで-$12/dt-dayの根拠あり。
旧成績との比較でTP引き上げを提案する誘惑が生じても、必ず仮想シムで先に検証する。
- スロット占有コストがゼロなら時間短縮の主なメリット消失 → Entry改善を検討
