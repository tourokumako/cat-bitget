# WORKFLOW.md — cat-bitget 作業フロー（2026-04-12）

過去実験結果・フェーズ構成・$60/day試算 → [WORKFLOW_ARCHIVE.md](WORKFLOW_ARCHIVE.md)

---

## Claudeの役割定義（最重要）

**ClaudeはPM・PL・SE・プログラマの全役割を担う。**

| 役割 | 担う内容 |
|------|---------|
| PM | 目標（$60/day）に向けた進捗管理・アプローチ見直し判断 |
| PL | 改善サイクルの設計・優先順位決定・リスク評価 |
| SE | コード・ロジック・パラメータの構造把握・仮説立案 |
| プログラマ | 実装・分析・テスト実行 |

**行動原則:**
- 指示を待たず、自分で現状を把握して「次に何をすべきか」を先に提案する
- 「何かおかしい」と感じたら自分で調べて根拠付きで指摘する
- ユーザーが気づく前に問題を発見する
- 仮説はデータで検証してから提示する（検証前の仮説は出さない）
- GOを待つのは「実装・コマンド実行・コミット」のみ。分析・指摘・提案は自律的に行う

---

## セッション開始プロトコル（毎回・例外なく実行）

新しいセッションが始まったら、ユーザーの指示を待たずに以下を実行する:

```
Step 0【PM視点・省略禁止】: 目標到達可能性チェック
  ① 目標 $60/day − 現在ベースライン（**180d/dayを使う。後半90dは過学習のため不可**） = 不足額を計算する
  ② 現行アプローチの最大改善幅を暗算する
       - 残存損失の削減上限（TIME_EXIT・MFE_STALE等）
       - 収益増の上限（件数増・TP改善等）
  ③ 判定:
       → 不足額を埋められる見込みあり: Step 1（タスク実行）へ進む
       → 構造的に届かない: アプローチ見直しをユーザーに提案してから STOP
  ④ 今セッションで着手するタスクを優先順位付きで提示する

Step 1. WORKFLOW.md を読む → 現在地・次のアクションを把握する
Step 2. 現在地のStepに必要なファイルを読む（コード・CSV・JSONを自律的に判断）
Step 3. 現状を自分でアセスメントして「次にやるべきこと・理由・懸念点」を提案する
```

**Step 0 の判断基準:**
- 「損失削減だけで届く」→ 損失削減タスクを優先
- 「損失削減だけでは届かない」→ 収益増施策（件数増・TP設計見直し）を先に提案する
- 「現行アプローチの最良ケースでも届かない」→ アプローチ見直しを先に決めてから個別タスクに入る

**Step 3 の判断基準:**
- 「次のアクション」に `grid_search.py` 実行が書かれている → GRIDを確認してそのまま実行提案する（追加分析は不要）
- 「次のアクション」に分析が書かれている → 分析してから仮説を提示する
- 「次のアクション」が空・不明 → 最大損失Priorityを特定してStep 2から提案する

ユーザーの最初の一言を待ってから動くのではなく、**セッション開始時に自分から状況報告と提案を出す。**

---

## 設計思想（絶対不変）

- タイムフレーム: P2/P4/P22/P23 = 5分足 / P1/P21 = 5分足（1m足はエッジゼロ確認済み）
- エントリーシグナル: V9ベース（P2/P4/P22/P23）/ MACD(12,26,9)クロス（P1/P21）
- TP幅: Priority単位で設定（型は事前定義せず検証結果から決定する。P2_TP_PCT=0.0006は現在の検証値）
- ポジションサイズ上限: 0.12 BTC/trade
- **各Priorityは独立したミニ戦略として個別に最適化する**

**【複数Priorityを立てる意図（絶対不変）】**
複数のエントリーシグナル（Priority）を並立させる目的は、
**ポジションを各Priorityに分散し、1点集中エントリーによる損失リスクを軽減するため。**
→ 改善に詰まったとき「1エントリーあたりのポジションサイズを増やして目標達成」は設計思想に反する。禁止。
→ 正しいアプローチは「新しいエッジのあるPriorityを追加して分散を増やす」こと。

---

## ⚠️ 新規Priority設計 vs 既存Priority改善（着手前に必ず判別する）

| 種別 | 対象 | やること |
|------|------|---------|
| **既存改善** | P2/P4/P22/P23 | 結果分析→仮説→グリッド→採用（下記サイクル通り） |
| **新規設計** | P1/P21など新規追加 | Entry/Exitロジックをゼロから設計する（下記「新規設計フロー」を使う） |

**新規Priority設計では「既存パラメータのチューニング」発想を捨てる。**
MFE_STALE/TIME_EXIT等の既存Exit機構をそのまま流用するのではなく、
「このシグナルの特性を最大化するにはどのExitが最適か」をゼロから問う。

---

## ⚠️ 新規Priority設計フロー（P1/P21等・新規追加時に使う）

```
Step N-1. 目標貢献額の逆算（着手前・必須）
          ① $60/day目標 - 既存策略合計 = 新Priorityに必要な貢献額
          ② 候補シグナルの最良ケース試算:
               件数上限（フィルターなし） × per-trade最大NET → X/day
               件数上限（フィルターあり） × per-trade最大NET → Y/day
          ③ X/day・Y/dayが必要貢献額に届かない場合
               → シグナル設計を変えるか・目標貢献額を再定義してからStep N-2へ
          ④ 届く場合のみ続行。この試算をユーザーに提示  ← STOP

Step N-2. Entryロジック設計
          【最初にやること：シグナルエッジ検証（実装前・必須）】
          実装に入る前に、候補シグナルの方向性エッジをスクリプト1本で確認する。
          - フィルターなし・タイトなTP/SL（例: TP=0.06%, SL=0.1%）でシミュレート
          - 勝率・per-trade期待値・件数/day を計測する
          - 勝率60%未満 または per-trade期待値 ≤ 0 → シグナル自体を見直す
          エッジが確認できた場合のみ以下に進む  ← STOP

          ① シグナルの特性を言語化する（モメンタム型・逆張り型・ブレイクアウト型等）
          ② その特性から「エッジが発生する条件」を仮説化する
          ③ フィルターは「品質向上」目的のみ。件数を潰すフィルターは費用対効果を試算してから採用
          設計案をユーザーに提示  ← STOP

Step N-3. Exitロジック設計（Entryと独立して設計する）【コードに触れる前に必須】
          ⚠️ コードを開く前にこのステップを完了すること。既存コードが目の前にあると
             「既存流用」の思考が自動的に始まる。設計を言語化してからコードに触れる。

          ① Entryシグナルの特性を言語化する
               「方向が合った時、価格はどう動くか？」
               「方向が外れた時、価格はどう動くか？」
          ② ①から損切りロジックを導く（既存流用ではなく特性から逆算する）
               モメンタム型（MACDクロス等）: 外れたら即逆行 → タイトなSL必須
               逆張り型（stoch等）: 一時的な逆行を許容 → 時間・MFE系が有効
          ③ TP・SL・時間カット等を全てゼロから決める（他Priorityのコピー禁止）
               既存機構（MFE_STALE/TIME_EXIT）を流用する場合は理由を明示する
               「他で使っているから」は理由にならない

          ⛔【L-89 再発防止】設計案に以下が含まれていたら提示前に削除して書き直す:
               × 「〇〇に準拠」「後で調整」「暫定値」
               × MFE_STALE gate を他Priorityからコピーした場合（必ず以下を計算すること）:
                   TP gross = POSITION_SIZE_BTC × 現在価格 × TP_PCT
                   推奨 gate = TP gross の 15〜25%（例: $18 TP → gate $3〜5）
                   推奨 hold_min = TIME_EXIT_MIN の 1/4〜1/3（例: 480min → 120〜160min）
               × 初回Replayが EV<0 でも「採用不可」を出さない
                   → グリッドサーチ完了後が採用判定のタイミング（L-89）

          設計案（Entry特性 → Exit導出の論拠込み）をユーザーに提示  ← STOP

Step N-4. 実装 → Replay → 評価
          採用基準: 必要貢献額を満たすか（NET>$0だけでは不十分）
```

---

## 設計フェーズ（Entry/Exitロジックが未確定・または抜本見直しが必要な場合）

**改善サイクルに入る前提条件: Entry/Exitロジックが確定していること。**
未確定・または目標達成に構造的に届かない場合はこのフェーズを先に完了する。

```
Step D-1. 目標逆算（必須・全Priority対象）
          ① $60/day - 他Priority合計 = 対象Priorityに必要な貢献額
          ② 現状のper-trade NET × 件数上限（フィルターなし）= 理論最大貢献額
          ③ 理論最大が必要貢献額に届かない → Entry/Exit設計を根本から変える必要あり
          ④ 各Priorityの「件数制約の原因」と「per-trade NET制約の原因」を特定する
          全Priority比較表をユーザーに提示  ← STOP

Step D-2. 価格挙動分析（コードより先・設計根拠を作る）
          ① 候補シグナル発火後のMFE分布（N分後に何%動くか）を計測する
          ② TP到達率を各TP幅で計算する（ATR比で判定）
          ③「取れる幅の上限」「取れる件数の上限」を数値で把握する

Step D-3. Entry設計（言語化フェーズ・コード禁止）
          ① シグナルの特性を言語化する
          ② エッジが発生する条件を特定する
          ③ 目標件数に対してフィルターの費用対効果を計算する
          設計案をユーザーに提示  ← STOP

Step D-4. Exit設計（言語化フェーズ・コード禁止）
          ① Entry特性から最適なExitを逆算する（既存流用禁止・特性から導く）
          ② TP幅・SL幅・時間カット・Trailを全てゼロから設計する
          設計案をユーザーに提示  ← STOP

Step D-5. 実装 → Replay → 評価
          採用基準: 必要貢献額を満たすか（NET>$0だけでは不十分）
          → 採用後: 改善サイクル（Step 1-8）へ移行（パラメータ微調整フェーズ）
```

---

## 改善サイクル（Entry/Exitロジック確定後のパラメータ最終調整）

```
Step 1.  構造把握 & 診断インフラ確認
                          【コード・ロジック把握】
                          cat_v9_decider.py: 対象PriorityのEntry条件・フィルタ・全パラメータの役割
                          replay_csv.py: Exit優先順位・各Exitの発動条件・パラメータ
                          cat_params_v9.json: 現在値の確認

                          【診断インフラ確認（必須）】
                          results CSVの出力項目が以下を満たしているか確認する:
                          □ mfe_usd（最大含み益）
                          □ mae_usd（最大含み損・逆行の深さ）
                          □ stoch_k/d_at_entry（シグナル強度）
                          □ bb_width_at_entry（ボラ幅）
                          □ ret_5（直近モメンタム）
                          □ exit_reason / hold_min / net_usd
                          不足項目があれば先にreplay_csv.pyに追加してReplayを実行する

                          ユーザーに構造サマリーを提示  ← STOP

Step 2.  結果分析        — results/replay_*.csv から対象Priorityの実数を取る

                          ⛔【グリッドサーチ前・省略禁止】トレードレベル分析
                          以下を全て完了するまで Step 3（仮説立案）に進まない:

                          ── A. なぜ負けているか ──
                          □ exit_reason別: 件数・NET・avg_MFE を計算する
                          □ SL_gross / TP_gross 比率を計算し「損益分岐勝率」を明示する
                               SL_PCT / TP_PCT = X倍 → 必要勝率 = X/(1+X)
                               例: SL=0.015, TP=0.008 → 1.875倍 → 必要勝率 65%
                          □ SL_FILLED の avg_MFE を確認する
                               （MFE≈0 → 即逆行 = エントリー品質問題）
                          □ TIME_EXIT の avg_MFE / TP_gross 比率を計算する
                               （30%未満 → シグナル品質問題 / 50〜80% → TPが遠すぎる可能性）

                          ── B. どうすれば勝てるか（勝ちを逃している原因）── ← 必須・省略禁止
                          □ TIME_EXIT の「惜しかった」件数を定量化する
                               MFE/TP_gross ≥ 70% の件数 → これが「TPに届きそうだった」件数
                               仮にTP幅を X% 縮小したら何件がTP_FILLEDに転換するか試算する
                          □ PROFIT_LOCK の機会損失を計算する
                               PROFIT_LOCK件数 × (TP_gross - avg_exit_net) = 逃した利益
                               ARM / TP_gross 比率を確認（80%以上ならTPの手前で常に発火する設計）
                          □ MFE_STALE_CUT の「早期切りすぎ」を確認する
                               MFE_STALE_CUT の avg_MFE を確認（gate値と乖離が大きいか）
                               もし avg_MFE > gate × 2 なら hold_min が短すぎる可能性
                          □ TP_FILLEDトレードのMFE分布を確認する
                               avg_MFE >> TP_gross なら「もっと長く持てた」余地あり
                               avg_MFE ≒ TP_gross なら「ちょうど良い」
                          □ 「勝ちを逃している主因」を1文で言語化する
                               × 「パラメータが悪い」はNG
                               ○ 「TIME_EXIT 7件中 4件がMFE/TP≥70%。TP幅を0.006に縮めれば転換可能」等

                          ── C. 総合判断 ──
                          □ A（損失原因）と B（機会損失原因）を並べて「どちらを先に解決すべきか」を判断する
                          □ 「なぜ負けているか」と「なぜ勝てないか」を各1文で言語化してから Step 3 へ進む

                          損失トレードのentry指標値（ADX・ATR・slope等）
                          ユーザーに結果サマリーを提示  ← STOP

Step 3.  仮説立案        — 構造 × 結果を照合して原因候補を特定し、必ずデータで根拠を確認する
                          ① 損失 vs 成功トレードの指標分布を比較する
                          ② 「差がある」だけでなく「その差が損失を説明できるか」を確認する
                             （価格が逆行したのか・TPに届かなかったのか・どちらが何件か）
                          ③ 候補パラメータを変えた場合の効果を事前推定する
                             （STALE削減件数 vs TP削減件数を計算してNET改善を試算）
                          ④ NET改善が見込める場合のみ仮説として採用する
                          根拠・推定効果なしの仮説は提示しない
                          仮説・根拠・推定効果をセットでユーザーに提示  ← STOP

                          【TIME_EXIT 改善 必須チェック（TIME_EXITが主損失の場合）】
                          以下5軸を全て検討し、有効な軸を**複数組み合わせて**グリッドサーチ設計する。
                          1軸単独のチューニングは原則禁止（相乗効果を狙う）。

                          | 軸 | 確認内容 | 候補パラメータ |
                          |----|---------|--------------|
                          | エントリー品質 | TIME_EXIT vs TP のentry指標分布を比較 → 弁別できる特徴量を特定 | ADX/ATR/stoch_k/bb_width等フィルター |
                          | ポジションサイズ | TIME_EXIT時のadd_count分布を確認 → 多addが損失を拡大しているか | MAX_ADDS_BY_PRIORITY |
                          | 中間EXIT追加 | 含み損拡大パターンを確認 → 新Exit条件（モメンタム消失・傾き転換）が有効か | replay_csv.pyロジック追加 |
                          | MFE_STALE調整 | TIME_EXIT前のMFE推移を確認 → 含み益消滅の早期検知が可能か | GATE_USD / HOLD_MIN |
                          | 保持時間 | TIME_EXIT_MINの短縮効果を確認（件数増加なら逆効果） | TIME_EXIT_MIN |

                          グリッドサーチはStep 4の【2段階グリッド法】に従い、
                          Phase 1は各軸2点（極端値）で方向を掴んでから、Phase 2で絞り込む。
                          軸の数は制限しない。値の数を絞ることで組み合わせ爆発を防ぐ。

Step 4.  グリッドサーチ設計・実行（必須・省略禁止）
                          【必須】パラメータ変更は必ず grid_search.py で最適値を探索してから
                                  cat_params_v9.json に反映する。直接書き換えは禁止。

                          【2段階グリッド法・必須】
                          1回の実行に数分〜数十分かかる。最初から細かい値を探索しない。
                          「方向を掴む → 周辺を絞る」の2段階で進める。

                          ▶ Phase 1（方向確認）: 各軸は3点（低・中・高/無効）
                            - 各軸: [強フィルター, 中間, 無効値(=フィルターなし)] の3点
                            - 例: stoch_k_max=[25, 30, 999]、bb_width=[0.010, 0.020, 0.030]、max_adds=[1, 3, 5]
                            - 組み合わせ数: 3^N（軸数N。3軸=27、4軸=81→軸を絞る）
                            - データ: **180d**（90d最適化は過学習リスクあり・禁止）
                            - 判定: どの軸がNETを改善するか方向性を特定する
                            - 効かない軸はPhase 2から除外する

                          ▶ Phase 2（絞り込み）: Phase 1ベスト付近のみ
                            - Phase 1で「効いた軸」のみ残す
                            - 有効な値域の間を3〜5点で細探索
                            - 組み合わせ数: 目安 ≤ 15

                          【採用前の個別検証】
                          - グリッドサーチ（180d）で最良値を特定後
                          - 前半90d・後半90d を個別Replayで確認 → 両方EV>0 が採用条件

                          TP_PCT変更時は手数料計算を先に実施（L-40）
                          他Priorityへの影響を確認する
                          採用基準: ① 現状ベースライン比でNET改善 ② 全体NETが改善 ③ per-trade NET > 手数料
                          grid_search.py の TARGET_PRIORITY と GRID を更新して実行コマンドをユーザーに提示  ← STOP

Step 5.  グリッドサーチ結果評価
                          ① 上位パターンの比較表
                          ② 推奨パラメータ（Before / After）
                          ③ 採用基準未達の場合 → 仮説を見直してStep 3へ戻る  ← STOP

Step 6.  実装            — パラメータ変更はStep 5承認後に直接適用
                          コード変更はdiffを提示してGO待ち  ← STOP

Step 7.  最終Replay      — Replayコマンドをユーザーに提示  ← STOP

Step 8.  採用判定        — 全項目チェック後「採用しますか？」← STOP
                          未達の場合 → Step 3へ戻り仮説見直し

Step 9.  baseline更新    — 採用時のみ更新
                          目標達成可能性チェック（毎回出力・省略禁止）:
                            ① 現在$Z/day → 目標$60/day → 残り不足額を計算する
                            ② 残り不足額を「現在進行中の施策の最良ケース」で埋められるか試算する
                            ③ 構造的に届かない場合 → アプローチ見直しをユーザーに提案する
                               （「NET>$0達成」≠「目標達成」。毎回この視点で評価する）

Step 10. Gitコミット     — ユーザーの明示OK後のみ  ← STOP
```

> **STOPルール**: 実装・コマンド実行・コミットは必ずユーザーのGO後に行う。
> 分析・指摘・提案は自律的に行う。

---

## Priority別ステータス（2026-04-21 レジーム分析後 全面見直し）

**【重要】前半/後半の区切りは時系列3か月区切りであり相場レジームとは無関係。**
**両期間ともSMA下方が主体（前半70%・後半100%）。レジーム別分析が正しいベースライン。**

### レジーム別実績（365d・MA70基準・全Priority有効化）

| Priority | downtrend/day | range/day | uptrend/day | 特性・判定 |
|---------|--------------|-----------|-------------|-----------|
| P23-SHORT | **+$8.03** | +$0.60 | -$3.70 | ✅ downtrend核心戦略 |
| P2-LONG | +$0.63 | -$1.35 | -$1.35 | ⚠️ downtrend方向あり・最適化要 |
| P3-LONG | +$0.24 | -$0.02 | -$0.26 | ⚠️ downtrend小プラス・最適化要 |
| P4-LONG | -$0.13 | **+$0.34** | -$1.55 | ⚠️ range方向あり・最適化要 |
| P24-SHORT | +$0.49 | -$1.46 | **+$1.67** | ⚠️ uptrend候補（33件と少ない・要検証）|
| P21-SHORT | +$0.59 | -$6.05 | -$3.05 | ❌ range件数爆発・設計見直し必須 |
| P22-SHORT | -$0.81 | -$1.12 | +$0.12 | ❌ 全レジームで赤字 |
| P1-LONG | -$1.73 | -$1.68 | +$0.11 | ❌ 全レジームで赤字（TP率2.6%）|

---

---

## ⛔ 暴走防止ルール（セッション開始時に必ず確認）

- **ロードマップから外れた作業を始めない**
- **ユーザーのGOなしに実装・実行しない**
- **1ターンで2つ以上の変更をしない**
- **目先の問題に反応して計画を変えない**

---

## 目標達成ロードマップ（2026-04-21 全面改訂）

```
Goal: NET $60/day

【根本方針（2026-04-21 確定）】
  - 日足MA70を基準にdowntrend/range/uptrendの3レジームに切り替える
  - 各レジームに方向性の合うPriorityを割り当て、個別に最適化・設計
  - 現状Priorityは「方向性が近いもの」をスタート地点として育てる
  - 最終検証: 365d Replay で GO/NO（ここでの再チューニング禁止）

【レジーム切り替え基準（2026-04-21 確定）】
  downtrend : 日足 MA70 下向き & close < MA70 & ADX≥20
  range     : 日足 ADX < 20
  uptrend   : 日足 MA70 上向き & close > MA70 & ADX≥20
  実装: run_once_v9.py 起動時に日足MA70+ADX計算 → Priority セット切り替え

【レジーム別 Priority 割り当て（スタート地点）】
  downtrend: P23（核心）・P2・P3 を育てる。P21は件数制御が課題
  range    : P4 を育てる。P24・P21・P23は無効化
  uptrend  : P24 を候補（33件と少なく要検証）。ほぼゼロから設計

【検証データセット】
  開発: 365d（2025/04-2026/04）+ MA70ウォームアップ用日足データ
  最終GO/NO判定: 365d Replay（再チューニング禁止）
```

---

## 現在地: レジーム切り替え実装 + downtrend最適化 ← 2026-04-21

**2026-04-21 セッションで確定した事項:**

| 発見 | 内容 |
|------|------|
| 前半/後半区切りは相場レジームと無関係 | 単純な時系列3か月区切り。SMAベースの分析が正しい |
| MA70がP23の性能分離に最適 | downtrend $8.03/day vs uptrend -$3.70/day（MA70基準）|
| 200SMAは切り替え基準として不適切 | データで確認済み。MA70を採用 |
| P23はdowntrendで圧倒的 | $8.03/day（MA70 downtrend）|
| P4はrangeでのみプラス | range $0.34/day。uptrend -$1.55/dayと逆相関 |
| P21はrange件数爆発 | range -$6.05/day（2189件）。レジーム切り替えで無効化が必須 |

**次のアクション（次セッション）:**
```
Step 1: MA70切り替えロジック実装
  - run_once_v9.py に日足MA70+ADX計算 → Priority セット切り替えを実装
  - regime_analysis.py でテスト確認

Step 2: downtrend セット最適化
  - P23: 現状 $8.03/day（ベースライン）→ さらに改善余地を探る
  - P2: downtrend $0.63/day → グリッドサーチで改善
  - P3: downtrend $0.24/day → TP/SL設計見直しで改善

Step 3: range セット（Step 2完了後）
  - P4: range $0.34/day → range特化で最適化

Step 4: uptrend セット（Step 3完了後）
  - P24: 33件と少ない → シグナル検証から開始
```

**現在のファイル状態（2026-04-21）:**
- `runner/regime_analysis.py`: 新規作成（全Priority × レジーム別パフォーマンス分析）
- `config/cat_params_v9.json`: ENABLE_P3_LONG=true（他は未変更）
- `runner/fetch_ohlcv.py`: --interval オプション追加済み（1d対応）
- `data/BTCUSDT-1d-2024-09-01_04-15_227d.csv`: MA70ウォームアップ用日足データ
- `data/BTCUSDT-5m-2025-04-01_03-31_365d.csv`: 365日データ（メイン検証用）

---

## Priority別 確定パラメータ（2026-04-18時点）

| Priority | パラメータ | 値 | 備考 |
|---------|-----------|-----|------|
| P2-LONG | P2_POSITION_SIZE_BTC | 0.06 | ✅採用 |
| P2-LONG | P2_TP_PCT | 0.0006 | 調整中 |
| P2-LONG | MAX_ADDS_BY_PRIORITY.2 | 1 | 採用済み |
| P2-LONG | P2_ATR14_MIN | **80.0** | ✅2026-04-17更新 |
| P2-LONG | P2_MFE_STALE_GATE_USD | 0.5 | 採用済み |
| P2-LONG | P2_MFE_STALE_HOLD_MIN | 90.0 | 採用済み |
| P2-LONG | P2_TIME_EXIT_MIN | 480 | 採用済み |
| P2-LONG | P2_ADX_EXCL_MAX | 200.0 | 採用済み |
| P2-LONG | P2_STOCH_K_MIN | **25.0** | ✅2026-04-14更新 |
| P22-SHORT | P22_ADX_MIN | 22.0 | 採用済み |
| P22-SHORT | P22_ADX_RELAX_MIN | **22.0** | ✅2026-04-14更新 |
| P22-SHORT | P22_TIME_EXIT_DOWN_FACTOR | **0.4** | ✅2026-04-17更新 |
| P23-SHORT | SHORT_POSITION_SIZE_BTC | 0.024 | 未スケール |
| P23-SHORT | **P23_TP_PCT** | **0.012** | ✅2026-04-18更新 |
| P23-SHORT | **P23_TIME_EXIT_MIN** | **480** | ✅2026-04-18更新（明示追加） |
| P23-SHORT | P23_ATR14_MIN | 150.0 | 採用済み |
| P23-SHORT | **P23_ADX_MAX** | **50.0** | ✅2026-04-20更新（40→50）|
| P23-SHORT | P23_MFE_STALE_GATE_USD | 4.0 | 採用済み |
| P23-SHORT | **P23_MFE_STALE_HOLD_MIN** | **30.0** | ✅2026-04-20更新（90→30）|
| P23-SHORT | P23_SHORT_PROFIT_LOCK_ENABLE | 0 | 採用済み（バグ修正） |
| P4-LONG | P4_TP_PCT | 0.003 | ✅採用 |
| P4-LONG | LONG_POSITION_SIZE_BTC | 0.024 | 未スケール |
| P4-LONG | P4_ATR14_MAX | 130.0 | 採用済み |
| P1-LONG | P1_ATR14_MAX | **9999** | ✅2026-04-16更新 |
| P1-LONG | P1_ADX_MIN | **30.0** | ✅2026-04-16更新 |
| P21-SHORT | P21_ATR14_MAX | **9999** | ✅2026-04-16更新 |
| P21-SHORT | P21_ADX_MIN | **0** | ✅2026-04-18更新（ADXフィルター撤廃） |
| P21-SHORT | **P21_SL_PCT** | **0.02** | ✅2026-04-18新設（Priority別SL対応）|
| P21-SHORT | **P21_TIME_EXIT_MIN** | **120** | ✅2026-04-18更新（480→120）|

---

## 現在のベースライン（PM基準: **180d/dayを使う**。後半90dは過学習確定・参考値のみ）

| Priority | 後半90d/day | 前半90d/day | **180d/day** | 主な損失 |
|---------|-----------|-----------|------------|---------|
| P2-LONG | +$14.1 | -$15.7 | **-$0.8** | 前半崩壊・過学習 |
| P4-LONG | +$3.8 | -$5.4 | **-$0.8** | 前半崩壊・過学習 |
| P22-SHORT | +$3.8 | -$5.4 | **-$0.8** | ADX上限修正で救済可能性あり |
| P23-SHORT | +$5.1 | +$12.5 | **+$8.8** | ✅2026-04-20更新。ADX_MAX=50・HOLD_MIN=30採用 |
| P1-LONG | +$2.4 | -$7.2 | **-$2.4** | エッジなし確認済み |
| P21-SHORT | +$7.2 | -$5.0 | **+$1.1** | EXIT設計見直し要 |
| **全体** | **+$43.0/day** | **-$36.9/day** | **≈ +$3.0/day** | |

> **Step 0 PM計算**: 目標 $60/day − 180dベースライン $3/day = **不足 $57/day**

---

## Replayコマンド

```bash
# 後半90日（開発・チューニング用）
echo "=====🚀 RUN START $(date) =====" && \
cd /Users/tachiharamasako/Documents/GitHub/cat-bitget/v3-bitget-api-sdk/bitget-python-sdk-api && \
python3 runner/replay_csv.py data/BTCUSDT-5m-2026-01-01_04-01_combined_90d.csv

# 前半90日（開発・チューニング用）
echo "=====🚀 RUN START $(date) =====" && \
cd /Users/tachiharamasako/Documents/GitHub/cat-bitget/v3-bitget-api-sdk/bitget-python-sdk-api && \
python3 runner/replay_csv.py data/BTCUSDT-5m-2025-10-03_12-31_first90d.csv

# 180日（中間チェック用）
echo "=====🚀 RUN START $(date) =====" && \
cd /Users/tachiharamasako/Documents/GitHub/cat-bitget/v3-bitget-api-sdk/bitget-python-sdk-api && \
python3 runner/replay_csv.py data/BTCUSDT-5m-2025-10-03_04-01_combined_180d.csv

# 365日（最終GO/NO判定・再チューニング禁止）
echo "=====🚀 RUN START $(date) =====" && \
cd /Users/tachiharamasako/Documents/GitHub/cat-bitget/v3-bitget-api-sdk/bitget-python-sdk-api && \
python3 runner/replay_csv.py data/BTCUSDT-5m-2025-04-01_03-31_365d.csv
```

## エッジ検証コマンド（採用前に必ず実行）

```bash
# P23-LONG/SHORT エッジ確認（前半）
python3 runner/p23_long_edge_check.py data/BTCUSDT-5m-2025-10-03_12-31_first90d.csv
# P23-LONG/SHORT エッジ確認（後半）
python3 runner/p23_long_edge_check.py data/BTCUSDT-5m-2026-01-01_04-01_combined_90d.csv
```
